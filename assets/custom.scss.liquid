/*============================================================================
Well Dressed Wolf Custom Theme - Clean
Author Aeolidia http://aeolidia.com

based on Shopify Timber v1.0 Copyright 2014 Shopify Inc.
==============================================================================*/

/*============================================================================
  Table of Contents

  #Breakpoint and Grid Variables
  #General Variables
  #Sass Mixins
  #Sass Mixins - Custom
  #Normalize
  #Grid Setup
  #Basic Styles
  #Helper Classes
  #Typography
  #Rich Text Editor
  #Links and Buttons
  #Lists
  #Tables
  #Reponsive Tables
  #OOCSS Media Object
  #Images and Iframes
  #Forms
  #Icons
  #Pagination
  #Site Header
  #Site Nav and Dropdowns
  #Mobile Nav
  #Drawers
  #Site Footer
  #Product and Collection Grids
  #Collection Filters
  #Breadcrumbs
  #Product Page
  #Info Pages
  #Account Pages
  #Blog
  #Notes and Form Feedback
  #Cart Page
  #Vendor
  #FancyBox Customizations
  #Slick Slider customizations
  #Bootstrap customizations
  #Ajax Cart Styles
==============================================================================*/

/*============================================================================
  #Breakpoint and Grid Variables
==============================================================================*/
$viewportIncrement: 1px;

$small: 700px;
$medium: 900px;
$large: 901px;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/*================ The following are dependencies of csswizardry grid ================*/
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

/*============================================================================
  #General Variables
==============================================================================*/

// Define colors
$colorPrimary: {{ settings.color_primary }};
$colorSecondary: {{ settings.color_secondary }};
$colorTertiary: {{ settings.color_tertiary }};
$colorQuaternary: {{ settings.color_quaternary }};
$colorQuinary: {{ settings.color_quinary }};
$colorSenary: {{ settings.color_senary }};
$colorSeptary: {{ settings.color_septary }};
$colorOctonary: #fff;
$colorNonary: #e7e7e7;
$colorDenary: {{ settings.color_denary }};

// Button colors
$colorBtnPrimary: $colorSecondary;
$colorBtnPrimaryHover: $colorTertiary;
$colorBtnPrimaryActive: $colorTertiary;
$colorBtnPrimaryText: $colorOctonary;

$colorBtnSecondary: $colorQuinary;
$colorBtnSecondaryHover: $colorQuaternary;
$colorBtnSecondaryActive: $colorQuaternary;
$colorBtnSecondaryText: $colorOctonary;

// Text link colors
$colorLinkPrimary: $colorSeptary;
$colorLinkPrimaryHover: $colorQuaternary;

$colorLinkSecondary: $colorQuaternary;
$colorLinkSecondaryHover: $colorSeptary;

$colorLinkTertiary: $colorPrimary;
$colorLinkTertiaryHover: $colorQuaternary;

$colorLinkQuaternary: $colorSecondary;
$colorLinkQuaternaryHover: $colorQuaternary;

// Text colors
$colorTextPrimary: $colorPrimary;
$colorTextSecondary: $colorSecondary;

// Border colors
$colorBorder: $colorSenary;

// Logo max width
$logoMaxWidth: 100%;

// Helper colors
$disabledGrey: #f6f6f6;
$disabledBorder: darken($disabledGrey, 25%);
$errorRed: #d02e2e;
$errorRedBg: #fff6f6;
$successGreen: #56ad6a;
$successGreenBg: #ecfef0;

// Drawers
$drawerNavWidth: 300px;
$drawerCartWidth: 300px;
$colorDrawers: $colorOctonary;
$colorDrawerBorder: $colorQuaternary;
$colorDrawerText: $colorPrimary;
$drawerTransition: all 0.4s cubic-bezier(0.46, 0.01, 0.32, 1);

// Sizing variables
$siteWidth: 1200px;
$gutter: 30px;
$gridGutter: 30px; // can be a %, but nested grids will have smaller margins because of it
$radius: 3px;

// Z-index
$zindexNavDropdowns: 5;
$zindexDrawer: 10;

/*================ Typography ================*/

/*! Generated by Font Squirrel (https://www.fontsquirrel.com) on May 9, 2017 */

@font-face {
  font-family: 'cooper_hewittmedium';
  src: url({{'cooperhewitt-medium-webfont.woff2' | asset_url }}) format('woff2'),
       url({{'cooperhewitt-medium-webfont.woff' | asset_url }}) format('woff');
  font-weight: normal;
  font-style: normal;
}

/*
@font-face {
    font-family: 'cooper_hewittsemibold';
    src: url({{'cooperhewitt-semibold-webfont.woff2' | asset_url }}) format('woff2'),
         url({{'cooperhewitt-semibold-webfont.woff' | asset_url }}) format('woff');
    font-weight: normal;
    font-style: normal;
}
*/

@font-face {
    font-family: 'cooper_hewittbold';
    src: url({{'cooperhewitt-bold-webfont.woff2' | asset_url }}) format('woff2'),
         url({{'cooperhewitt-bold-webfont.woff' | asset_url }}) format('woff');
    font-weight: normal;
    font-style: normal;
}

/**
 * @license
 * MyFonts Webfont Build ID 3387033, 2017-05-10T11:57:48-0400
 * 
 * The fonts listed in this notice are subject to the End User License
 * Agreement(s) entered into by the website owner. All other parties are 
 * explicitly restricted from using the Licensed Webfonts(s).
 * 
 * You may obtain a valid license at the URLs below.
 * 
 * Webfont: BodoniEgyptianPro-Light by ShinnType
 * URL: https://www.myfonts.com/fonts/shinn/bodoni-egyptian-pro/light/
 * Copyright: Copyright (c) 2010 by Nick Shinn. Published by Shinn Type Foundry. All rights reserved.
 * Licensed pageviews: 10,000
 * 
 * 
 * 
 * License: https://www.myfonts.com/viewlicense?type=web&buildid=3387033
 * 
 * copyright 2017 MyFonts Inc
*/

/* @import must be at top of file, otherwise CSS will not work */
@import url("//hello.myfonts.net/count/33ae99");

/**
 * @license
 * MyFonts Webfont Build ID 3390325, 2017-05-16T11:35:25-0400
 * 
 * The fonts listed in this notice are subject to the End User License
 * Agreement(s) entered into by the website owner. All other parties are 
 * explicitly restricted from using the Licensed Webfonts(s).
 * 
 * You may obtain a valid license at the URLs below.
 * 
 * Webfont: BodoniEgyptianPro-Medium by ShinnType
 * URL: https://www.myfonts.com/fonts/shinn/bodoni-egyptian-pro/medium/
 * Copyright: Copyright (c) 2010 by Nick Shinn. Published by Shinn Type Foundry. All rights reserved.
 * Licensed pageviews: 20,000
 * 
 * 
 * License: https://www.myfonts.com/viewlicense?type=web&buildid=3390325
 * 
 * copyright 2017 MyFonts Inc
*/

/* @import must be at top of file, otherwise CSS will not work */
@import url("//hello.myfonts.net/count/33bb75");

@font-face {
  font-family: 'BodoniEgyptianPro-Medium';
  src: url({{'33BB75_0_0.eot' | asset_url }});
  src: url({{'33BB75_0_0.eot?#iefix' | asset_url }}) format('embedded-opentype'),
       url({{'33BB75_0_0.woff2' | asset_url }}) format('woff2'),
       url({{'33BB75_0_0.woff' | asset_url }}) format('woff'),
       url({{'33BB75_0_0.ttf' | asset_url }}) format('truetype');
     }

@font-face {
    font-family: 'BodoniEgyptianPro-Light';
    src: url({{'33AE99_0_0.eot' | asset_url }});
    src: url({{'33AE99_0_0.eot?#iefix' | asset_url }}) format('embedded-opentype'),
         url({{'33AE99_0_0.woff2' | asset_url }}) format('woff2'),
         url({{'33AE99_0_0.woff' | asset_url }}) format('woff'),
         url({{'33AE99_0_0.ttf' | asset_url }}) format('truetype');
    }
  
/*
 *
 * All OpenType features and all extended glyphs have been removed.
 *
 * The fonts included in this stylesheet are subject to the End User License you purchased
 * from Yellow Design Studio. The fonts are protected under domestic and international trademark and 
 * copyright law. You are prohibited from modifying, reverse engineering, duplicating, or
 * distributing this font software.
 *
 * (c) 2010-2014 Yellow Design Studio
 *
 *
 *
 *
 */

@font-face {
  font-family: 'thirsty_softlight';
  src: url({{'thirstysoftlight-webfont.eot' | asset_url }});
  src: url({{'thirstysoftlight-webfont.eot?#iefix' | asset_url }}) format('embedded-opentype'),
       url({{'thirstysoftlight-webfont.woff' | asset_url }}) format('woff'),
       url({{'thirstysoftlight-webfont.ttf' | asset_url }}) format('truetype'),
       url({{'thirstysoftlight-webfont.svg#thirsty_softlight' | asset_url }}) format('svg');
  font-weight: normal;
  font-style: normal;
}

$bodyFontStack: 'cooper_hewittmedium', 'Helvetica Neue', Helvetica, Arial, sans-serif;
$bodyFontStack2: 'cooper_hewittbold', 'Helvetica Neue', Helvetica, Arial, sans-serif;
$bodyFontStack3: 'BodoniEgyptianPro-Light', 'Helvetica Neue', Helvetica, Arial, sans-serif;
$bodyFontStack4: 'BodoniEgyptianPro-Medium', 'Helvetica Neue', Helvetica, Arial, sans-serif; 
$headerFontStack: 'thirsty_softlight', 'Helvetica Neue', Helvetica, Arial, sans-serif;

$socialIconFontStack: 'FontAwesome';

$baseFontSize: 10px; // Henceforth known as 1em

html {
  font-size: $baseFontSize; // Sets base font size to 1rem/10px for simpler rem values
}

@mixin font-size($sizeValue: 1.6) { // Calculate font sizes using rem with px fallback
  font-size: ($sizeValue * 10) + px;
  font-size: $sizeValue + rem;
}

/* Type styles */

@mixin typestyle-body { // Cooper Hewitt, 14px
  font-family: $bodyFontStack;
  @include font-size(1.4);
  font-weight: normal;
}

@mixin typestyle-body2 { // Cooper Hewitt Bold, 12px, uppercase
  font-family: $bodyFontStack2;
  @include font-size(1.2);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .09em;
}

@mixin typestyle-body3 { // Bodoni, 20px
  font-family: $bodyFontStack3;
  @include font-size(2.0);
  font-weight: normal;
  text-transform: none;
  line-height: 1.4;  
}

@mixin typestyle-body4 { // Cooper Hewitt Bold, 11px, uppercase
  font-family: $bodyFontStack2;
  @include font-size(1.1);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .07em;
}

@mixin typestyle-body5 { // Cooper Hewitt, 10px, uppercase
  font-family: $bodyFontStack;
  @include font-size(1.0);
  font-weight: normal;
  text-transform: uppercase;
  letter-spacing: .1em;
}

@mixin typestyle-body6 { // Cooper Hewitt, 11px, uppercase
  font-family: $bodyFontStack;
  @include font-size(1.1);
  font-weight: normal;
  text-transform: uppercase;
  letter-spacing: .09em;
}

@mixin typestyle-body7 { // Cooper Hewitt, 11px
  font-family: $bodyFontStack;
  @include font-size(1.1);
  font-weight: normal;
  letter-spacing: .05em;
}

@mixin typestyle-body8 { // Bodoni, 16px
  font-family: $bodyFontStack3;
  @include font-size(1.6); 
  font-weight: normal;
  text-transform: none;  
}

@mixin typestyle-body9 { // Cooper Hewitt, 12px
  font-family: $bodyFontStack;
  @include font-size(1.2);
  font-weight: normal;
  letter-spacing: .09em;
}

@mixin typestyle-body10 { // Cooper Hewitt Bold, 10px, uppercase
  font-family: $bodyFontStack2;
  @include font-size(1.0);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .07em;
}

@mixin typestyle-heading1 { // Bodoni, 30px
  font-family: $bodyFontStack3;
  @include font-size(3.0); 
  font-weight: normal;
  text-transform: none;  
}

@mixin typestyle-heading2 { // Thirsty, 24px
  font-family: $headerFontStack;
  @include font-size(2.4);
  font-weight: normal;
  text-transform: none;
  letter-spacing: 0;
}

@mixin typestyle-heading3 { // Cooper Hewitt, 18px
  font-family: $bodyFontStack;
  @include font-size(1.8);
  font-weight: normal;
  text-transform: none;
  letter-spacing: .06em;
}

@mixin typestyle-heading4 { // Bodoni Medium, 11px, uppercase
  font-family: $bodyFontStack4;
  @include font-size(1.1);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .13em;
}

@mixin typestyle-heading5 { // Thirsty, 14px
  font-family: $headerFontStack;
  @include font-size(1.5);
  font-weight: normal;
  text-transform: none;
  letter-spacing: 0;
}

@mixin typestyle-heading6 { // Bodoni Medium, 12px, uppercase
  font-family: $bodyFontStack4;
  @include font-size(1.2);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .08em;
}

@mixin typestyle-heading7 { // Bodoni, 18px
  font-family: $bodyFontStack3;
  @include font-size(1.8); 
  font-weight: normal;
  text-transform: none;  
  line-height: 1.5;
}

@mixin typestyle-heading8 { // Bodoni Medium, 15px, uppercase
  font-family: $bodyFontStack4;
  @include font-size(1.5);
  font-weight: normal;
  text-transform: uppercase;  
  letter-spacing: .08em;
}

@mixin typestyle-heading9 { // Bodoni, 36px
  font-family: $bodyFontStack3;
  @include font-size(3.6); 
  font-weight: normal;
  text-transform: none;  
  letter-spacing: 0;
}

@mixin typestyle-heading10 { // Bodoni Medium, 16px
  font-family: $bodyFontStack4;
  @include font-size(1.6);
  font-weight: normal;
  text-transform: none;  
}

/*============================================================================
  #Sass Mixins
==============================================================================*/
.clearfix {
  &:after {
    content: '';
    display: table;
    clear: both; }
  *zoom: 1;
}

@mixin clearfix() {
  &:after {
    content: '';
    display: table;
    clear: both; }
  *zoom: 1;
}

/*============================================================================
  Prefixer mixin for generating vendor prefixes:
    - Based on https://github.com/thoughtbot/bourbon/blob/master/app/assets/stylesheets/addons/_prefixer.scss
    - Usage:

      // Input:
      .element {
        @include prefixer(transform, scale(1), ms webkit spec);
      }

      // Output:
      .element {
        -ms-transform: scale(1);
        -webkit-transform: scale(1);
        transform: scale(1);
      }
==============================================================================*/

@mixin prefixer($property, $value, $prefixes) {
  @each $prefix in $prefixes {
    @if $prefix == webkit {
      -webkit-#{$property}: $value;
    } @else if $prefix == moz {
      -moz-#{$property}: $value;
    } @else if $prefix == ms {
      -ms-#{$property}: $value;
    } @else if $prefix == o {
      -o-#{$property}: $value;
    } @else if $prefix == spec {
      #{$property}: $value;
    } @else  {
      @warn "Unrecognized prefix: #{$prefix}";
    }
  }
}

@mixin transform($transform) {
  @include prefixer(transform, $transform, ms webkit spec);
}

@mixin user-select($value: none) {
  @include prefixer(user-select, $value, webkit moz ms spec);
}

@mixin backface($visibility: hidden) {
  @include prefixer(backface-visibility, $visibility, webkit spec);
}

@function em($target, $context: $baseFontSize) {
  @if $target == 0 {
    @return 0;
  }
  @return $target / $context + 0em;
}

@function color-control($color) {
  @if (lightness( $color ) > 40) {
    @return #000;
  }
  @else {
    @return #fff;
  }
}

/*============================================================================
  Layer promotion mixin for creating smoother animations with higher FPS.
==============================================================================*/
@mixin promote-layer($properties: transform) {
  -webkit-transform: translateZ(0); // translateZ hack
  will-change: $properties; // spec
}

/*============================================================================
  Dependency-free breakpoint mixin
    - Based on http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
    - Usage docs: http://shopify.github.io/Timber/#sass-mixins
==============================================================================*/
$min: min-width;
$max: max-width;
@mixin at-query ($constraint_, $viewport1_, $viewport2_:null) {
 $constraint: $constraint_; $viewport1: $viewport1_; $viewport2: $viewport2_;
  @if type-of($constraint_) == number {
    $viewport1 : $constraint_; $viewport2 : $viewport1_; $constraint : null;
  }
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

/*============================================================================
  #Sass Mixins - Custom
==============================================================================*/

/* Reset ul
  Usage:  @include ul-reset;
*/

@mixin ul-reset {
    list-style: none;
    margin-left: 0;
    margin-right: 0;
    padding: 0;   

    & > li {
      margin: 0;
      padding: 0;
      background-image: none;
    }
}

/*============================================================================
  #Normalize
==============================================================================*/

/*============================================================================
  #Grid Setup
    - Based on csswizardry grid, but with floated columns, a fixed gutter size, and BEM classes
    - Breakpoints defined above, under #Breakpoint and Grid Variables
    - Note the inclusion of .grid-uniform to take care of clearfixes on evenly sized grid items
==============================================================================*/
$responsive:         true;
$mobile-first:       true;
$use-silent-classes: true;
$push:               true;
$pull:               false;

/* Force clearfix on grids */
.grid,
.grid-uniform {
  @include clearfix;
}

/* Manual grid__item clearfix */
.grid__item.clear {
  clear: both;
}

$class-type: unquote(".");

@if $use-silent-classes == true {
  $class-type: unquote("%");
}

@mixin grid-media-query($media-query) {
  $breakpoint-found: false;

  @each $breakpoint in $breakpoints {
    $name: nth($breakpoint, 1);
    $declaration: nth($breakpoint, 2);

    @if $media-query == $name and $declaration {
      $breakpoint-found: true;

      @media only screen and #{$declaration} {
        @content;
      }
    }
  }

  @if $breakpoint-found == false {
    @warn "Breakpoint '#{$media-query}' does not exist";
  }
}


/*============================================================================
  Drop relative positioning into silent classes which can't take advantage of
  the `[class*="push--"]` and `[class*="pull--"]` selectors.
==============================================================================*/
@mixin silent-relative() {
  @if $use-silent-classes == true {
    position:relative;
  }
}

/*============================================================================
  Grid Setup
    1. Allow the grid system to be used on lists.
    2. Remove any margins and paddings that might affect the grid system.
    3. Apply a negative `margin-left` to negate the columns' gutters.
==============================================================================*/
#{$class-type}grid,
#{$class-type}grid-uniform {
  list-style: none;
  margin: 0;
  padding: 0;
  margin-left: -$gridGutter;
}

#{$class-type}grid__item {
  box-sizing: border-box;
  float: left;
  min-height: 1px;
  padding-left: $gridGutter;
  vertical-align: top;
  @if $mobile-first == true {
    width: 100%;
  }
}

/*============================================================================
  Reversed grids allow you to structure your source in the opposite
  order to how your rendered layout will appear.
==============================================================================*/
#{$class-type}grid--rev {
  @extend #{$class-type}grid;
  direction: rtl;
  text-align: left;

  > #{$class-type}grid__item {
    direction: ltr;
    text-align: left;
    float: right;
  }
}

/* Gutterless grids have all the properties of regular grids, minus any spacing. */
#{$class-type}grid--full {
  @extend #{$class-type}grid;
  margin-left: 0;

  > #{$class-type}grid__item {
    padding-left: 0;
  }
}

/*============================================================================
  WIDTHS
    - Create width classes, prefixed by the specified namespace.
==============================================================================*/
@mixin device-type($namespace:"") {
  /** Whole */
  #{$class-type}#{$namespace}one-whole       { width: 100%; }

  /* Halves */
  #{$class-type}#{$namespace}one-half        { width: 50%; }

  /* Thirds */
  #{$class-type}#{$namespace}one-third       { width: 33.333%; }
  #{$class-type}#{$namespace}two-thirds      { width: 66.666%; }

  /* Quarters */
  #{$class-type}#{$namespace}one-quarter     { width: 25%; }
  #{$class-type}#{$namespace}two-quarters    { width: 50%; }
  #{$class-type}#{$namespace}three-quarters  { width: 75%; }

  /* Fifths */
  #{$class-type}#{$namespace}one-fifth       { width: 20%; }
  #{$class-type}#{$namespace}two-fifths      { width: 40%; }
  #{$class-type}#{$namespace}three-fifths    { width: 60%; }
  #{$class-type}#{$namespace}four-fifths     { width: 80%; }

  /* Sixths */
  #{$class-type}#{$namespace}one-sixth       { width: 16.666%; }
  #{$class-type}#{$namespace}two-sixths      { width: 33.333%; }
  #{$class-type}#{$namespace}three-sixths    { width: 50%; }
  #{$class-type}#{$namespace}four-sixths     { width: 66.666%; }
  #{$class-type}#{$namespace}five-sixths     { width: 83.333%; }

  /* Eighths */
  #{$class-type}#{$namespace}one-eighth      { width: 12.5%; }
  #{$class-type}#{$namespace}two-eighths     { width: 25%; }
  #{$class-type}#{$namespace}three-eighths   { width: 37.5%; }
  #{$class-type}#{$namespace}four-eighths    { width: 50%; }
  #{$class-type}#{$namespace}five-eighths    { width: 62.5%; }
  #{$class-type}#{$namespace}six-eighths     { width: 75%; }
  #{$class-type}#{$namespace}seven-eighths   { width: 87.5%; }

  /* Tenths */
  #{$class-type}#{$namespace}one-tenth       { width: 10%; }
  #{$class-type}#{$namespace}two-tenths      { width: 20%; }
  #{$class-type}#{$namespace}three-tenths    { width: 30%; }
  #{$class-type}#{$namespace}four-tenths     { width: 40%; }
  #{$class-type}#{$namespace}five-tenths     { width: 50%; }
  #{$class-type}#{$namespace}six-tenths      { width: 60%; }
  #{$class-type}#{$namespace}seven-tenths    { width: 70%; }
  #{$class-type}#{$namespace}eight-tenths    { width: 80%; }
  #{$class-type}#{$namespace}nine-tenths     { width: 90%; }

  /* Twelfths */
  #{$class-type}#{$namespace}one-twelfth     { width: 8.333%; }
  #{$class-type}#{$namespace}two-twelfths    { width: 16.666%; }
  #{$class-type}#{$namespace}three-twelfths  { width: 25%; }
  #{$class-type}#{$namespace}four-twelfths   { width: 33.333%; }
  #{$class-type}#{$namespace}five-twelfths   { width: 41.666% }
  #{$class-type}#{$namespace}six-twelfths    { width: 50%; }
  #{$class-type}#{$namespace}seven-twelfths  { width: 58.333%; }
  #{$class-type}#{$namespace}eight-twelfths  { width: 66.666%; }
  #{$class-type}#{$namespace}nine-twelfths   { width: 75%; }
  #{$class-type}#{$namespace}ten-twelfths    { width: 83.333%; }
  #{$class-type}#{$namespace}eleven-twelfths { width: 91.666%; }
}

/*================ Clearfix helper on uniform grids ================*/
@mixin clearfix-helper($namespace:"") {
  .grid-uniform {
    #{$class-type}#{$namespace}one-half:nth-child(2n+1),
    #{$class-type}#{$namespace}one-third:nth-child(3n+1),
    #{$class-type}#{$namespace}one-quarter:nth-child(4n+1),
    #{$class-type}#{$namespace}one-fifth:nth-child(5n+1),
    #{$class-type}#{$namespace}one-sixth:nth-child(6n+1),
    #{$class-type}#{$namespace}two-sixths:nth-child(3n+1),
    #{$class-type}#{$namespace}three-sixths:nth-child(2n+1),
    #{$class-type}#{$namespace}two-eighths:nth-child(4n+1),
    #{$class-type}#{$namespace}four-eighths:nth-child(2n+1),
    #{$class-type}#{$namespace}five-tenths:nth-child(2n+1),
    #{$class-type}#{$namespace}one-twelfth:nth-child(12n+1),
    #{$class-type}#{$namespace}two-twelfths:nth-child(6n+1),
    #{$class-type}#{$namespace}three-twelfths:nth-child(4n+1),
    #{$class-type}#{$namespace}four-twelfths:nth-child(3n+1),
    #{$class-type}#{$namespace}six-twelfths:nth-child(2n+1)    { clear: both; }
  }
}

/*================ Helper show/hide classes around our breakpoints ================*/
@mixin device-helper($namespace:"") {
  #{$class-type}#{$namespace}show        { display: block!important; }
  #{$class-type}#{$namespace}hide        { display: none!important; }

  #{$class-type}#{$namespace}text-left   { text-align: left!important; }
  #{$class-type}#{$namespace}text-right  { text-align: right!important; }
  #{$class-type}#{$namespace}text-center { text-align: center!important; }

  #{$class-type}#{$namespace}left        { float: left!important; }
  #{$class-type}#{$namespace}right       { float: right!important; }
}

/*================ Our regular, non-responsive width and helper classes ================*/
@include device-type();
@include device-helper();

/*================ Our responsive classes, if we have enabled them ================*/
@if $responsive == true {
  @each $name in $breakpoint-has-widths {
    @include grid-media-query($name) {
      @include device-type('#{$name}--');
      @include device-helper('#{$name}--');
      @include clearfix-helper('#{$name}--');
    }
  }
}

/*============================================================================
  PUSH
    - Push classes, to move grid items over to the right by certain amounts
==============================================================================*/
@mixin push-setup($namespace: "") {
  /* Whole */
  #{$class-type}push--#{$namespace}one-whole       { left: 100%; @include silent-relative(); }

  /* Halves */
  #{$class-type}push--#{$namespace}one-half        { left: 50%; @include silent-relative(); }

  /* Thirds */
  #{$class-type}push--#{$namespace}one-third       { left: 33.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-thirds      { left: 66.666%; @include silent-relative(); }

  /* Quarters */
  #{$class-type}push--#{$namespace}one-quarter     { left: 25%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-quarters    { left: 50%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-quarters  { left: 75%; @include silent-relative(); }

  /* Fifths */
  #{$class-type}push--#{$namespace}one-fifth       { left: 20%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-fifths      { left: 40%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-fifths    { left: 60%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-fifths     { left: 80%; @include silent-relative(); }

  /* Sixths */
  #{$class-type}push--#{$namespace}one-sixth       { left: 16.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-sixths      { left: 33.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-sixths    { left: 50%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-sixths     { left: 66.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}five-sixths     { left: 83.333%; @include silent-relative(); }

  /* Eighths */
  #{$class-type}push--#{$namespace}one-eighth      { left: 12.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-eighths     { left: 25%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-eighths   { left: 37.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-eighths    { left: 50%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}five-eighths    { left: 62.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}six-eighths     { left: 75%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}seven-eighths   { left: 87.5%; @include silent-relative(); }

  /* Tenths */
  #{$class-type}push--#{$namespace}one-tenth       { left: 10%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-tenths      { left: 20%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-tenths    { left: 30%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-tenths     { left: 40%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}five-tenths     { left: 50%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}six-tenths      { left: 60%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}seven-tenths    { left: 70%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}eight-tenths    { left: 80%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}nine-tenths     { left: 90%; @include silent-relative(); }

  /* Twelfths */
  #{$class-type}push--#{$namespace}one-twelfth     { left: 8.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-twelfths    { left: 16.666%; @include silent-relative();  }
  #{$class-type}push--#{$namespace}three-twelfths  { left: 25%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-twelfths   { left: 33.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}five-twelfths   { left: 41.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}six-twelfths    { left: 50%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}seven-twelfths  { left: 58.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}eight-twelfths  { left: 66.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}nine-twelfths   { left: 75%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}ten-twelfths    { left: 83.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}eleven-twelfths { left: 91.666%; @include silent-relative(); }
}

@if $push == true {
  [class*="push--"]{ position:relative; }

  @include push-setup();

  @if $responsive == true {
    @each $name in $breakpoint-has-push {
      @include grid-media-query($name) {
        @include push-setup('#{$name}--');
      }
    }
  }
}

/*============================================================================
  PULL
    - Pull classes, to move grid items back to the left by certain amounts
==============================================================================*/
@mixin pull-setup($namespace: "") {
  /* Whole */
  #{$class-type}pull--#{$namespace}one-whole       { right: 100%; @include silent-relative(); }

  /* Halves */
  #{$class-type}pull--#{$namespace}one-half        { right: 50%; @include silent-relative(); }

  /* Thirds */
  #{$class-type}pull--#{$namespace}one-third       { right: 33.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-thirds      { right: 66.666%; @include silent-relative(); }

  /* Quarters */
  #{$class-type}pull--#{$namespace}one-quarter     { right: 25%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-quarters    { right: 50%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-quarters  { right: 75%; @include silent-relative(); }

  /* Fifths */
  #{$class-type}pull--#{$namespace}one-fifth       { right: 20%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-fifths      { right: 40%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-fifths    { right: 60%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-fifths     { right: 80%; @include silent-relative(); }

  /* Sixths */
  #{$class-type}pull--#{$namespace}one-sixth       { right: 16.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-sixths      { right: 33.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-sixths    { right: 50%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-sixths     { right: 66.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}five-sixths     { right: 83.333%; @include silent-relative(); }

  /* Eighths */
  #{$class-type}pull--#{$namespace}one-eighth      { right: 12.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-eighths     { right: 25%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-eighths   { right: 37.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-eighths    { right: 50%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}five-eighths    { right: 62.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}six-eighths     { right: 75%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}seven-eighths   { right: 87.5%; @include silent-relative(); }

  /* Tenths */
  #{$class-type}pull--#{$namespace}one-tenth       { right: 10%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-tenths      { right: 20%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-tenths    { right: 30%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-tenths     { right: 40%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}five-tenths     { right: 50%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}six-tenths      { right: 60%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}seven-tenths    { right: 70%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}eight-tenths    { right: 80%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}nine-tenths     { right: 90%; @include silent-relative(); }

  /* Twelfths */
  #{$class-type}pull--#{$namespace}one-twelfth     { right: 8.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-twelfths    { right: 16.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-twelfths  { right: 25%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-twelfths   { right: 33.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}five-twelfths   { right: 41.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}six-twelfths    { right: 50%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}seven-twelfths  { right: 58.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}eight-twelfths  { right: 66.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}nine-twelfths   { right: 75%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}ten-twelfths    { right: 83.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}eleven-twelfths { right: 91.666%; @include silent-relative(); }
}

@if $pull == true {
  [class*="pull--"]{ position:relative; }

  @include pull-setup();

  @if $responsive == true {
    @each $name in $breakpoint-has-pull {
      @include grid-media-query($name) {
        @include pull-setup('#{$name}--');
      }
    }
  }
}

/*============================================================================
  #Basic Styles
==============================================================================*/

html,
body {
  background-color: $colorOctonary;
}

.wrapper {
  max-width: $siteWidth;
}

.main-content {
  margin-top: 0;
  padding-bottom: 60px;

  .template-product & {
    margin-top: 20px;
  }

  .template-search &,
  .template-list-collections &,
  .template-cart & {
    margin-top: $gutter;
  }
}

.footer-content-wrapper,
.page-content-wrapper {
  @extend %grid; // Grid wrappers 
  @include clearfix(); // Clear after the grid
}

.header-content-wrapper {
  @extend %grid--full; // Grid wrappers 
  @include clearfix(); // Clear after the grid
}

.page-content-wrapper {
  .page-content,
  .sidebar {
    @extend %grid__item; // .page-content-wrapper grid
  }

  .page-content {
    .template-blog &,
    .template-article &{
      @extend %large--two-thirds; // Width of the grid item at large breakpoint
    }

    &.page-contact,
    .template-search & {
      @extend %large--two-thirds;
      @extend %push--large--one-sixth;
    }

    .template-customers-register &,
    .template-customers-login &,
    .template-customers-activate_account &,
    .template-customers-reset_password &  {
      @extend %large--one-third;
      @extend %push--large--one-third;
    }

    .template-customers-account &,
    .template-customers-addresses &,
    .template-customers-order & {
      @extend %large--two-thirds;
    }
  }

  .sidebar {
    .template-blog &,
    .template-article & {
      @extend %large--one-third; // Width of the grid item at large breakpoint
    }

    .template-customers-account &,
    .template-customers-addresses &,
    .template-customers-order & {
      @extend %large--one-third;
    }
  }
}

.template-index,
.template-collection,
.template-product,
.template-page,
.template-page-contact,
.template-cart,
.template-page-mission,
.template-page-about,
.template-customers-account,
.template-customers-register,
.template-customers-login,
.template-customers-order,
.template-customers-addresses,
.template-customers-account,
.template-customers-activate_account,
.template-customers-reset_password,
.template-customers-activate_account { /* Wide section pages */
  .main-content.wrapper { /* Wide section pages */
    max-width: 100%;
    padding-left: 0;
    padding-right: 0;
  }
}

/*============================================================================
  #Helper Classes
==============================================================================*/

.is-transitioning {
  display: block !important;
  visibility: visible !important;
}

.display-table {
  display: table;
  table-layout: fixed;
  width: 100%;
}

.display-table-cell {
  display: table-cell;
  vertical-align: middle;
  float: none;
}

@include at-query ($min, $large) {
  .large--display-table {
    display: table;
    table-layout: fixed;
    width: 100%;
  }

  .large--display-table-cell {
    display: table-cell;
    vertical-align: middle;
    float: none;
  }
}

.visually-hidden {
  position: absolute;
  overflow: hidden;
  clip: rect(0 0 0 0);
  height: 1px; width: 1px;
  margin: -1px; padding: 0; border: 0;
}

/*============================================================================
  #Typography
==============================================================================*/

body,
input,
textarea,
button,
select {
  @include typestyle-body;
  color: $colorTextPrimary;
  //-webkit-font-smoothing: none;
}

h1, h2 {
  @include typestyle-heading1; 
}

h3, h4, h5, h6 {
  @include typestyle-heading2; 
}

.h1 { @extend h1; }
.h2 { @extend h2; }
.h3 { @extend h3; }
.h4 { @extend h4; }
.h5 { @extend h5; }
.h6 { @extend h6; }

/*================ Blockquotes ================*/

/*================ Code ================*/

/*================ Horizontal Rules ================*/

/*================ Section Headers ================*/
.section-header {
  text-align: center;
  background-color: $colorDenary;
  padding-top: 40px;
  padding-bottom: 40px;
  margin-bottom: 50px;
  color: $colorSecondary;

   .template-cart & {
    background-color: $colorOctonary;
    margin-bottom: 0;
  }
  
  .section-header__title,
  h1 {
    margin-bottom: 0;
  }
}

@include at-query ($min, $large) {
  .section-header__title,
  h1 {
    margin-bottom: 0;
  }

  .section-header__right {
    @include at-query ($max, $medium) {
      //margin-bottom: $gutter;
    }
  }
}

/*============================================================================
  #Rich Text Editor
==============================================================================*/

.rte {
  line-height: 1.75;
  //margin-bottom: $gutter / 2;

  strong,
  b {
    font-family: $bodyFontStack2;
    font-weight: normal;
  }

  a {
    color: $colorLinkSecondary;
    //text-decoration: underline;
    &:hover,
    &:focus {
      color: $colorLinkSecondaryHover;
    }
  }

  p {
    margin-bottom: 25px;
  }

  p:last-child {
    margin-bottom: 0;
  }

  h1, .h1, h2, .h2, h3, .h3, h4, .h4, h5, .h5, h6, .h6 {
    margin-top: .5em;
  }


  h1 {
    @include typestyle-heading1;
  }

  h2 {
    @include typestyle-heading2;
    margin-bottom: .25em;
  }

  h3 {
    @include typestyle-heading8;
  }

  h4 {
    @include typestyle-body2;
  }

  h5 {
    @include typestyle-heading6;
  }

  h6 {
    @include typestyle-heading5;
  }


}

/*============================================================================
  #Links and Buttons
==============================================================================*/

a,
.text-link {
  color: $colorLinkPrimary;
}

a:hover,
a:focus {
  color: $colorLinkPrimaryHover;
}

.btn,
.rte .btn {
  @include typestyle-body2;
  padding: 14px 8px 12px 10px;
  //line-height: 1.42;
  //font-weight: normal;
  border-radius: 0;

  /*================ Set primary button colors - can override later ================*/
  background-color: $colorBtnPrimary;
  color: $colorBtnPrimaryText;

  &:hover {
    background-color: $colorBtnPrimaryHover;
    color: $colorBtnPrimaryText;
  }

  &:active,
  &:focus {
   background-color: $colorBtnPrimaryActive;
   color: $colorBtnPrimaryText;
   outline: none;
  }

  &[disabled],
  &.disabled {
    //color: $colorOctonary;
    //background-color: $colorTertiary;

    color: $colorOctonary;
    background-color: $colorQuaternary;
  }
}

.btn--secondary,
.rte .btn--secondary {
  @include typestyle-body2;
    padding: 14px 10px 12px 10px;
  background-color: $colorBtnSecondary;
  border-radius: 0;

  &:hover {
    background-color: $colorBtnSecondaryHover;
    color: $colorBtnSecondaryText;
  }

  &:active,
  &:focus {
   background-color: $colorBtnSecondaryActive;
   color: $colorBtnSecondaryText;
   outline: none;
  }
}

.btn--small {
  //padding: 4px 5px;
  font-size: em(12px);
}

.btn--large {
  //padding: 12px 15px;
  font-size: em(16px);
}

.btn--full {
  width: 100%;
}

.toggle {
  &:hover {
    cursor: pointer;
  }
}

.toggle {
  color: $colorLinkQuaternary;

  &:hover,
  &:focus {
    color: $colorLinkQuaternaryHover;
    outline: none;
  }
}

/*============================================================================
  #Lists
==============================================================================*/

/*============================================================================
  #Tables
==============================================================================*/

th, td {
  border: 1px solid $colorBorder;
}

/*============================================================================
  Responsive tables, defined with .table--responsive on table element.
  Only defined for IE9+
==============================================================================*/

/*============================================================================
  #OOCSS Media Object
    - http://www.stubbornella.org/content/2010/06/25/the-media-object-saves-hundreds-of-lines-of-code/
==============================================================================*/

/*============================================================================
  #Images and Iframes
==============================================================================*/

/*============================================================================
  #Forms
==============================================================================*/

input,
textarea,
select {
  border: 1px solid $colorSenary;
  padding: 12px 10px 10px 10px;
  border-radius: 0;
  @include font-size(1.6); // 16px to avoid iOS zoom

  @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
    @include typestyle-body;
  }

  &:focus {
    //border: 1px solid darken($colorBorder, 10%);
  }

  &[disabled],
  &.disabled {
    background-color: $disabledGrey;
    border-color: $disabledBorder;
  }
}

textarea,
input,
button,
select {
  &:focus {
    outline: 2px solid $colorQuinary; 
  }
}

select {
  background: {
    image: url('{{ "select_arrow.svg" | asset_url }}');
    position: right 12px center;
  }
  padding-right: 45px;
}

::-webkit-input-placeholder { 
  color: $colorSeptary;
}
::-moz-placeholder { 
 color: $colorSeptary;
 opacity: 1;
}
:-ms-input-placeholder { 
 color: $colorSeptary
}
:-moz-placeholder { 
 color: $colorSeptary;
}

/*================ Input element overrides ================*/

/*================ Form labels ================*/

/*================ Horizontal Form ================*/

/*================ Error styles ================*/

/*================ Input Group ================*/
.input-group {

  .input-group-field:first-child,
  .input-group-btn:first-child,
  .input-group-btn:first-child > .btn,
  input[type="hidden"]:first-child + .input-group-field,
  input[type="hidden"]:first-child + .input-group-btn > .btn {
    border-radius: 0;
  }

  .input-group-field:last-child,
  .input-group-btn:last-child > .btn {
    border-radius: 0;
  }
}

/*============================================================================
  #Icons
==============================================================================*/

.supports-fontface .icon:before {
  font-family: $socialIconFontStack;
}

/*================ Icon mapping ================*/
.icon-amazon_payments:before { content: "\f270"; }
.icon-american_express:before { content: "\f1f3"; }
.icon-arrow-down:before { content: "\f107"; }
.icon-bitcoin:before { content: "\f15a"; }
.icon-cart:before { content: "\f07a"; }
//.icon-cirrus:before { content: "\43"; }
//.icon-dankort:before { content: "\64"; }
.icon-diners_club:before { content: "\f24c"; }
.icon-discover:before { content: "\f1f2"; }
.icon-email:before { content: "\f0e0"; }
.icon-facebook:before { content: "\f09a"; }
.icon-facebook-alt:before { content: "\f082"; }
//.icon-fancy:before { content: "\46"; }
.icon-google:before { content: "\f0d4"; }
.icon-google_wallet:before { content: "\f1ee"; }
.icon-grid-view:before { content: "\f0ce"; }
.icon-hamburger:before { content: "\f0c9"; }
.icon-instagram:before { content: "\f16d"; }
//.icon-interac:before { content: "\49"; }
//.icon-jcb:before { content: "\4a"; }
.icon-list-view:before { content: "\f039"; }
//.icon-maestro:before { content: "\6d"; }
.icon-master:before { content: "\f1f1"; }
.icon-minus:before { content: "\f068"; }
.icon-paypal:before { content: "\f1ed"; }
.icon-pinterest:before { content: "\f231"; }
.icon-plus:before { content: "\f067"; }
.icon-rss:before { content: "\f143"; }
.icon-search:before { content: "\f002"; }
.icon-stripe:before { content: "\f1f5"; }
.icon-tumblr:before { content: "\f174"; }
.icon-twitter:before { content: "\f099"; }
.icon-twitter-alt:before { content: "\f081"; }
.icon-vimeo:before { content: "\f194"; }
.icon-visa:before { content: "\f1f0"; }
.icon-x:before { content: "\f00d"; }
.icon-youtube:before { content: "\f166"; }

.social-icons li {
  .icon {
    @include font-size(1.6); // 30px
    line-height: 1;
  }

  a {
    color: $colorLinkSecondary;

    &:hover {
      color: $colorLinkSecondaryHover;
    }
  }
}
/*============================================================================
  #Pagination
==============================================================================*/

.pagination {
  @include typestyle-body4;
  text-align: right;
  color: $colorSeptary;

  &.pagination-top {
    margin-bottom: 50px;
  }

  &.pagination-bottom {
    margin-top: 50px;
    margin-bottom: 0;
  }

  .page.current {
    color: $colorQuaternary;
  }

  a, 
  .page.current {
    padding: 3px;
  }
}

/*============================================================================
  #Site Header
==============================================================================*/

.site-header {
  padding: 0;
}

.topbar {
  background-color: $colorSenary;
  border-bottom: 1px solid $colorQuinary;

  .wrapper {
    padding-top: 11px;
    padding-bottom: 7px;
  }

  &:after {
    height: 2px;
    display: block;
    background: $colorOctonary;
    content: '';
  }

  a {
    color: $colorLinkSecondary;

    &:hover,
    &:focus {
      color: $colorLinkSecondaryHover;
    }
  }

  .topbar-content {
    @extend %grid; // Grid wrappers 
    @include clearfix(); // Clear after the grid
  }

  .brands,
  .tagline {
    @extend %grid__item; 
    @extend %large--one-half;
    text-align: center;
  }

  ul,
  p {
    margin-bottom: 0;
    line-height: 1;
  }

  ul {
    @include ul-reset;
    @include typestyle-body4;

    li {
      display: inline-block;

      &:after {
        font-family: $socialIconFontStack;
        @include font-size(.5); 
        color: $colorOctonary;
        content: "\f111";
        padding-right: 6px;
        padding-left: 8px;
        vertical-align: top;
        margin-top: 3px;
        display: inline-block;
      }

      &:last-child,
      &.label {
        &:after {
          content: '';
          padding: 0;
        }
      }
    }
  }

  .label,
  .tagline {
    @include typestyle-heading5;
    color: $colorQuaternary;
  }

  .label {
    padding-right: 14px;
  }

  .brands {
    margin-bottom: 10px;

    .label {
      display: block;
      margin-bottom: 10px;

      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        display: inline-block;
        margin-bottom: 0;
      }
    }

    @include at-query ($min, $large) { // Show style at large breakpoint only
      text-align: left;
      margin-bottom: 0;
    }
  }
    
  .tagline {
    @include at-query ($min, $large) { // Show style at large breakpoint only
      text-align: right;
    }
  }
}

.header-content-wrapper {
  padding-top: 32px;
  padding-bottom: 32px;
  display: -ms-flexbox;
  display: -webkit-flex;
  display: flex;
  -ms-flex-align: center;
  -webkit-align-items: center;
  -webkit-box-align: center;
  align-items: center;

  @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
    padding-bottom: 25px;
  }
}

.header-search,
.header-logo,
.cartlinks {
  @extend %grid__item; // .header-content-wrapper grid
}

.header-search,
.cartlinks {
  @extend %large--one-quarter;
  @extend %medium--one-quarter;
  padding-top: 5px;
}

.header-logo {
  @extend %large--one-half;
  @extend %medium--one-half;
}

.header-search,
.cartlinks,
.nav-menu {
  @extend %small--hide;
  //@extend %medium--hide; 
}

.mobile-nav-menu {
  @extend %large--hide;
  @extend %medium--hide;
}

.cartlinks {
  @include typestyle-body5;
  text-align: right;

  div {
    display: inline-block;

      &.account-links {
        margin-right: 0;
        display: block;
        margin-bottom: 3px;

        @media only screen and (min-width: 910px) {
          margin-right: 10px;
          display: inline-block;
          margin-bottom: 0;
        }

        a {
          margin-right: 10px;

          &:last-child {
            margin-right: 0;

            @media only screen and (min-width: 910px) {
              margin-right: 10px;
            }
          }
        }
    }
  }

  #customer_logout_link {
    margin-left: 10px;
  }
}

.cartlinks,
.cart-button {
  svg {
    vertical-align: bottom;
    margin-right: 5px;
    .st0{fill:$colorQuinary;}

    &:hover {
      .st0{fill:$colorQuaternary;}
    }
  }
}

.site-header__logo {
  img {
    margin: 0 auto;

    &.default {
      width: 400px;
    }
  }
}

.site-header__logo-link {
  max-width: $logoMaxWidth;
  margin: 0 auto;
}

.site-header__cart-toggle {
  //display: inline-block;
}

.site-header__search {
  //display: inline-block;
  //max-width: 400px;
  //margin-top: $gutter / 2;
  //width: 100%;
}

.search-bar {
  input,
  button {
    border: none;
    background-color: $colorOctonary;
    padding: 0;
  }

  button {
    font-size: 16px;
    padding-left: 0;
    padding-right: 5px;
    color: $colorQuinary;

    &:hover,
    &:focus {
      background-color: $colorOctonary;
      color: $colorLinkSecondaryHover;
    }
  }

  input {
    @include typestyle-body;
    padding-left: 5px;
    @include font-size(1.6); // 16px to avoid iOS zoom

    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      @include typestyle-body5;
    }

    &:focus {
      border: none;
      outline: none;
      background-color: lighten($colorSenary, 3%);
    }
  }
}

.mobile-nav-menu {
  border-top: 1px solid $colorSenary;
  padding: 10px 0;
}

.mobile-nav-wrapper {
  @extend %grid; // Grid wrapper
  @include clearfix(); 

  #menu-icon {
    vertical-align: top;
    margin-right: 6px;
    display: inline-block;
    width: 24px;
    height: 15px;
    position: relative;
    //margin: 50px auto;
    -webkit-transform: rotate(0deg);
    -moz-transform: rotate(0deg);
    -o-transform: rotate(0deg);
    transform: rotate(0deg);
    -webkit-transition: .5s ease-in-out;
    -moz-transition: .5s ease-in-out;
    -o-transition: .5s ease-in-out;
    transition: .5s ease-in-out;
    cursor: pointer;
  }

  #menu-icon span {
    display: block;
    position: absolute;
    height: 2px;
    width: 100%;
    background: $colorPrimary;
    border-radius: 2px;
    opacity: 1;
    left: 0;
    -webkit-transform: rotate(0deg);
    -moz-transform: rotate(0deg);
    -o-transform: rotate(0deg);
    transform: rotate(0deg);
    -webkit-transition: transform .25s ease-in-out;
    -moz-transition: transform .25s ease-in-out;
    -o-transition: transform .25s ease-in-out;
    transition: transform .25s ease-in-out;
  }

  #menu-icon span:nth-child(1) {
    top: 2px;
  }

  #menu-icon span:nth-child(2),
  #menu-icon span:nth-child(3) {
    top: 8px;
  }

  #menu-icon span:nth-child(4) {
    top: 14px;
  }

  #menu-icon.open span:nth-child(1) {
    top: 6px;
    width: 0%;
    left: 50%;
  }

  #menu-icon.open span:nth-child(2) {
    -webkit-transform: rotate(45deg);
    -moz-transform: rotate(45deg);
    -o-transform: rotate(45deg);
    transform: rotate(45deg);
  }

  #menu-icon.open span:nth-child(3) {
    -webkit-transform: rotate(-45deg);
    -moz-transform: rotate(-45deg);
    -o-transform: rotate(-45deg);
    transform: rotate(-45deg);
  }

  #menu-icon.open span:nth-child(4) {
    top: 12px;
    width: 0%;
    left: 50%;
  }

  button:focus {
    outline: none;
  }

  button.js-drawer-open-left:hover {
    #menu-icon span {
      background: $colorQuaternary;
    }
  }

  .menu-button, 
  .cart-button {
    @extend %grid__item;
    @extend %one-half;
  }

/*
  .icon {
    @include font-size(1.8);
    display: inline-block;
    margin-right: 5px;
    vertical-align: middle;
    margin-bottom: -2px;
  }*/

  .site-nav__link {
    @include font-size(1.3);
    padding: 0;
  }

  button {
    margin-bottom: 0;
  }


  .menu-button {
    text-align: left;

  }

  .cart-button {
    text-align: right;

    svg {
      margin-bottom: 4px;
    }
  }
}

/*============================================================================
  #Site Nav and Dropdowns
==============================================================================*/

.nav-bar {
  background-color: $colorOctonary;
  border-bottom: 1px solid $colorQuinary;

  .template-index &,
  .template-collection &,
  .template-page &,
  .template-page-mission &,
  .template-page-about &,
  .template-customers-account &,
  .template-customers-register &,
  .template-customers-login &,
  .template-customers-order &,
  .template-customers-addresses &,
  .template-customers-account &,
  .template-customers-activate_account &,
  .template-customers-reset_password &,
  .template-page-contact & {
    border-bottom: none;
  }
}

.site-nav {
  text-align: center;
  //font-size: em(16px);
  //margin: 0 0 0 (-$gutter / 2);

  li {
    //margin: 0;
    //display: block;
  }
}

.site-nav, .site-nav--mobile {
  margin: 0 0 0 0;
}

.site-nav--active>.site-nav__link {
  font-weight: normal;
}

/*================ Home to hamburger and cart that toggle drawers ================*/


.site-nav__link {
  @include typestyle-heading4;
  color: $colorLinkPrimary;
  padding: 0 12px 5px 12px;

  &:hover,
  &:active,
  &:focus {
    color: $colorLinkPrimaryHover;
  }

  .site-nav--active > & {
    //font-weight: bold;
  }

  .icon-arrow-down {
    top: 2.5px;
    font-size: 15px;
    padding-left: 5px;
    color: $colorQuinary;
  }
}

/*================ Dropdowns ================*/

.site-nav__dropdown {
  a {
    background-color: $colorOctonary;
    @include typestyle-body5;

    &:hover,
    &:active,
    &:focus {
      background-color: $colorOctonary;
    }
  }
}

/*================ Search bar in header ================*/

.nav-search {
  //position: relative;
  //padding: 10px 0;

  @include at-query ($max, $medium) {
    //padding: 0 0 ($gutter / 2);
    //margin: 0 auto;
    //text-align: center;
  }
}

/*============================================================================
  #Mobile Nav
  - List of items inside the mobile drawer
==============================================================================*/

.mobile-nav__item {
  @include typestyle-heading7;

  // Background color on top level items so there is no
  // element overlap on subnav's CSS toggle animation
  .mobile-nav > & {
    background-color: $colorDenary;
  }

  &:after {
    border-bottom: none;
  }
}

.mobile-nav__item a, 
.mobile-nav__toggle button {
  padding: 15px 15px 10px 15px;
}

.mobile-nav__toggle button {
  padding: 15px 15px 10px 30px;
}

.mobile-nav {
  margin-bottom: 20px;

  &.mobile-main,
  &.mobile-search,
  &.mobile-account {
    padding-top: 10px;
    padding-bottom: 10px;
    background-color: $colorDenary;
    margin-bottom: 0;
    margin-top: 0;

    .mobile-nav__item {
      background-color: $colorDenary;
      //@include typestyle-body2;
      //text-align: center;
    }
  }

  &.mobile-main {
    .mobile-nav__item {
      &:after {
        border-bottom: 1px solid $colorQuinary;
      }
    }

    .mobile-nav__sublist {
      .mobile-nav__item {
        @include typestyle-body6;

        &:after {
          border-bottom: none;
        }

        a {
          padding: 0 0 10px 25px;
        }

        &:last-child {
          a {
            padding-bottom: 25px;
          }
        }
      }
    }
  }

  &.mobile-search {
    padding-top: 20px;

    button,
    input[type="search"] {
      padding-right: 10px;
      padding-left: 10px;
      border: none;
    }

    input[type="search"] {
      margin-left: -1px;
    }

  }

  &.mobile-account {
    .mobile-nav__item {
      @include typestyle-body8;

      a {
        padding: 10px 15px 5px 15px;
      }
    }
  }
}

.mobile-nav__item a,
.mobile-nav__toggle button {
  color: $colorLinkTertiary;

  &:hover,
  &:active,
  &:focus {
    color: $colorLinkTertiaryHover;
  }

  &:active,
  &:focus {
    background-color: $colorDenary;
  }
}

.mobile-nav__item--active {
  //font-weight: bold;
}

/*============================================================================
  #Drawers
==============================================================================*/

#PageContainer.zoom-enabled {
  .template-product & {  // Required for Magic Zoom to prevent zoomed photo from overlaying header dropdown menu on touch. Closing zoom when tapping outside would be better, but not included in this version of MZ
    z-index: 11111111111;
    position: relative;
  }
}

.drawer {
  z-index: 111111111111 !important; // Required for Magic Zoom to prevent zoomed photo from overlaying mobile menu

  color: $colorDrawerText;
  background-color: $colorDenary;

  input,
  textarea {
    border-color: $colorDrawerBorder;
  }
}

.drawer--left {
  border-right: 1px solid $colorDrawerBorder;
}

.drawer--right {
  border-left: 1px solid $colorDrawerBorder;
}

.drawer__header {
  border-bottom: none;
  height: 30px;
  margin-bottom: 0;
}

.drawer__close button {
  top: 10px;
  right: -15px;
  vertical-align: middle;

  &:active,
  &:focus {
    background-color: darken($colorDrawers, 5%);
  }
}

.close-icon-x {
  span {
    display: block;
    position: absolute;
    height: 2px;
    width: 80%;
    background: $colorPrimary;
    border-radius: 2px;
    opacity: 1;
    left: 0;
  }

  span:nth-child(1) {
    top: 6px;
    width: 0%;
    left: 50%;
  }

 span:nth-child(2) {
    -webkit-transform: rotate(45deg);
    -moz-transform: rotate(45deg);
    -o-transform: rotate(45deg);
    transform: rotate(45deg);
  }

  span:nth-child(3) {
    -webkit-transform: rotate(-45deg);
    -moz-transform: rotate(-45deg);
    -o-transform: rotate(-45deg);
    transform: rotate(-45deg);
  }

 span:nth-child(4) {
    top: 12px;
    width: 0%;
    left: 50%;
  }
}

.drawer__close button {
  &:active,
  &:focus {
    background-color: darken($colorDrawers, 5%);
  }
}

/*============================================================================
  #Site Footer
==============================================================================*/

.site-footer {
  text-align: center;
  background-color: transparent;
  color: $colorPrimary;
  padding-top: 2px;
  border-top: 1px solid $colorSenary;

  &:before {
    height: 10px;
    display: block;
    content: '';
    border-top: 10px solid $colorSenary;
  }

  .site-footer2 {
    margin-top: 2px;
    border-top: 1px solid $colorSenary;
  }

  @include at-query ($min, $large) {
    //padding: ($gutter * 2) 0;
  }
}

.footer-menu,
.footer-newsletter,
.credit {
  padding-top: $gutter;

  @extend %grid__item; // .footer-content-wrapper grid

  @include at-query ($min, $large) { // Show style at large breakpoint only
    padding-top: 50px;
    padding-bottom: 25px;
  }
}

.footer-menu {
  @extend %large--one-sixth;
  @extend %medium--one-third;
  text-align: center;

  @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
    text-align: left;
  }

  h3 {
    @include typestyle-heading6;
    color: $colorSecondary;
    margin-bottom: 20px;
  }

  ul {
    @include ul-reset;
    @include typestyle-body6;
    margin-bottom: 10px;

    li {
      margin-bottom: 15px;
    }
  }

  .social-icons {
    li {
      margin-right: 8px;
    }

    a {
      color: $colorLinkPrimary;

      &:hover,
      &:focus {
        color: $colorLinkPrimaryHover;
      }
    }
  }
}

.footer-newsletter {
  @extend %large--one-half;

  h3 {
    color: $colorSecondary;
    margin-bottom: 0;
    line-height: 1.2;
  }

  p {
    @include typestyle-heading7;
    margin-bottom: 8px;
  }

  .newsletter {
    @extend %grid--full;
    @include clearfix();

    @include at-query ($min, $large) { // Show style at large breakpoint only
      padding-top: 140px;
    }

    .signup,
    .img-wrapper {
      @extend %grid__item; 
    }

    .signup {
      @extend %large--two-thirds;
    }

    .img-wrapper {
      @extend %large--one-third;

      img {
        max-width: 100px;
        margin: 10px 0 0 10px;

        @include at-query ($min, $large) { // Show style at large breakpoint only
          margin: 0;
        }
      }
    }

    @include at-query ($min, $large) { // Show style at large breakpoint only
      padding: 10px 0 10px 80px;
      background-position: right top;
    }
  }

  .input-group .btn, .input-group .btn--secondary, .input-group .input-group-field {
    height: 32px;
  }

  input[type="email"] {
    @include typestyle-body7;
    @include font-size(1.6); // 16px to avoid iOS zoom
    padding: 0 10px 0 10px;

    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      @include typestyle-body7;
    }
  }

  form {
    max-width: 300px;
    margin: 0 auto;

    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      max-width: 280px;
    }
  }

  .btn {
    padding-left: 20px;
    padding-right: 20px;
    border-right: none;
    padding-top: 0;
    padding-bottom: 0;
  }
}

.credit {
  text-align: center;
  @include typestyle-body5;
  color: $colorSeptary;

  @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
    text-align: left;
  }
}

/*============================================================================
  #Product and Collection Grids
==============================================================================*/

.product-grid-item {
  text-align: center;
  margin-bottom: 70px;

  &:hover {
    // default is no hover effect
    // if browser doesn't have touch support show effect
    // if touch browser accidentally didn't have touch support it would show the effect (bad)
    // if non touch browser accidentally did have touch effect it would not show effect (fine)
    .supports-no-touch & {
      img {
        opacity: .5;
      }
    }
  }

  .grid__image {
    margin-bottom: 18px;
    position: relative;

    .badges,
    .flags {
      @extend %small--hide;
    }

    .badges {
      position: absolute;
      top: 5px;
      right: 5px;

      p {
        margin: 0 0 8px 0;
        @include typestyle-body5;
        
        &.sale span {
          line-height: 50px;
        }

        &.sold-out span {
          line-height: 1.1;
          padding-top: 15px;
        }

        span {
          padding-left: 2px;
          text-align: center;
          display: inline-block;
          background-color: $colorQuaternary;
          height: 50px;
          width: 50px;
          border-radius: 100%;
          color: $colorOctonary;
        }
      }
    }

    .flags {
      position: absolute;
      bottom: 0;
      width: 100%;
      //margin-top: -20px;
      background-color: $colorSecondary;
      color: $colorOctonary;

      p {
        margin: 8px 0;
        @include typestyle-body5;
      }
    }
  }

  p {
    @include typestyle-body8;

    &.price {
      @include typestyle-body4;
      margin-bottom: 0;
    }
  }
}

.grid__image {
  //margin: 0 auto ($gutter / 2);
}

.template-search {
  .page-content {
    form {
      border: 1px solid $colorQuinary;

      button {
        padding-left: 10px;
        padding-right: 10px;
      }

      input[type="search"] {
        padding-left: 10px;
      }
    }

    h2.h3 {
      @include typestyle-body2;
    }

    hr {
      border-top: 1px solid $colorQuinary;
    }
  }
}

.template-list-collections {
  hr {
    border-top: 1px solid $colorSenary;
    margin: 0 0 70px 0;
  }

  .section-header {
    background-color: $colorOctonary;
    padding-top: 0;
    padding-bottom: 0;

    .section-header__left {
      @include at-query ($min, $large) { // Show style at large breakpoint only
        text-align: left;
      }
    }
  }
} 

/*============================================================================
  #Collection Filters
==============================================================================*/

.collections-nav {
  @extend %small--hide; // Hide on small breakpoint

  ul { // All menu levels
    @include ul-reset;
  }

  li {
    &.active > a,
    &.active > a:visited {
    }
  }

  .has-dropdown ul { // Level 2 and 3
    display: none;
  }

  .has-dropdown.active ul {
    display: block;
  }

  & > ul > li { // Level 1
    margin-bottom: 1em;

    a {
    }

    & > a:hover {
    }
  }

  & > ul ul { // Level 2
    margin-left: 1em;

    li { 
      a,
      a:visited {
        &:hover {
        }
      }
    }
  }
}

/*============================================================================
  #Breadcrumbs
==============================================================================*/

.breadcrumb {
  //margin-bottom: $gutter;
  text-align: left;
  @include typestyle-body6;

  a,
  span {
    padding: 0 3px 0 0;
    margin-right: 3px;

    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      padding: 0 7px 0 0;
      margin-right: 7px;
    }
  }
}

/*============================================================================
  #Product Page
==============================================================================*/

.product-content {
  @extend %grid; // Grid wrappers 
  @include clearfix(); // Clear after the grid
  .social-icons {
    text-align: right;

    .label {
      @include typestyle-body2;
      color: $colorSeptary;
      padding-top: 10px;
      margin-right: 15px;
    }

    li {
      margin-right: 20px;

      &:last-child {
        margin-right: 0;
      }

      a {
        color: lighten($colorQuaternary, 6%);

        &:hover,
        &:focus {
          color: $colorQuaternary;
        }
      }
    }

    .icon {
      @include font-size(2.5);
    }
  }
}

.product-images,
.product-info {
  @extend %grid__item; // .product-content grid
}

.product-images {
  @extend %large--seven-twelfths; 
  @extend %medium--one-half; 

  .sharing {
    @extend %small--hide;
  }
}

.product-info {
  @extend %large--five-twelfths;
  @extend %medium--one-half;

  .sharing {
    @extend %medium--hide;
    @extend %large--hide;
    text-align: center;
    margin-top: $gutter;
  }
}

.product-wrapper {
  @media only screen and (min-width: 1200px) {
    padding-left: 55px;
    padding-top: 65px;
  }

  @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
    padding-top: 65px;
  }

  .vendor {
    @include typestyle-body5;
    margin-bottom: 15px;
  }

  .vendor,
  h1,
  .price {
    text-align: center;
  }

  h1,
  .price,
  .vendor {
    color: $colorSecondary;
  }

  h1 {
    margin-bottom: 10px;
  }

  .price {
    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      margin-bottom: 60px;
    }

    span,
    p {
      display: inline-block;
    }

    .h2 {
      @include typestyle-heading3;
    }
  }

  #ComparePrice {
    text-decoration: line-through;
  }

  .form-vertical {
    margin-bottom: $gutter;
  }

  .quantity-selector {
    @include typestyle-body5;
  }

  input#Quantity {
    max-width: 70px;
  }

  .selector-wrapper label {
    display: none;
  }

  select {
    @include typestyle-body;
    @include font-size(1.6); // 16px to avoid iOS zoom
    color: $colorSeptary;

    @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
      @include typestyle-body2;
    }

    padding-right: 0;

    option {
      text-transform: none;
    }
  }

  //select { width: 400px;}
  .tempforSize {
    display: inline-block !important;
  }

  .product-customizer-option {
    margin-top: 10px;
    margin-bottom: 10px;

    label {
      @include typestyle-body6;
      display: inline-block;
      margin-bottom: 5px;
    }
  }
 
  select,
  input,
  button {
    width: 100%;
  }

  .toggles {
  }

  h5.toggle {
    border-top: 1px solid $colorSenary;
    padding-top: 15px;
    padding-bottom: 13px;
    margin-bottom: 0;
    margin-top: 0;
    @include typestyle-body2;
    background-image: url({{ 'arrow_down.svg' | asset_url }});
    background-size: 14.9px 9px;
    background-position: center right;
    background-repeat: no-repeat;

    &:last-of-type {
      border-bottom: 1px solid $colorSenary;

      &.active {
        border-bottom: none;
      }
    }

    &:focus,
    &:hover {
      outline: none;
      color: $colorLinkQuaternaryHover;

      &:after {
        color: $colorLinkQuaternaryHover;
      }
    }

    &.active {
      background-image: url({{ 'arrow_up.svg' | asset_url }});
    }      
  }

  .toggled-content {
    padding-bottom: 15px;

    &:last-of-type {
      border-bottom: 1px solid $colorSenary;
    }
  }
}

.product-images {
  text-align: center;

  .product-single__thumbnails a {
    display: inline-block; // Fix for margin issue with MagicZoom
  }

  .mz-thumb:focus {
    border: 2px solid $colorQuinary;
  }
}

.has-thumbs {
 @extend %grid; 
 @include clearfix();

  &.is-zoom {
    @extend %grid--full; 

    .product-single__thumbnails {
      @include at-query ($min, $large) { // Show style at large breakpoint only
        margin-right: $gutter;
      }
    }
  }

  .product-single__photos,
  .thumbs {
    @extend %grid__item;
  }

  .thumbs {
    @include at-query ($min, $large) { // Show style at large breakpoint only
      width: 14%;
    }

    .grid__item {
      margin-bottom: $gutter;

      @include at-query ($min, $large) { // Show style at large breakpoint only
        margin-bottom: 15px;
      }
    }

    img {
    }

    .product-single__thumbnails a {
    }
  }

  .product-single__photos {
    @include at-query ($min, $large) { // Show style at large breakpoint only
      width: 86%;
    }
  }
}

.product-single__photos {
  //margin-bottom: $gutter;
}

.product-single__photos,
.product-single__thumbnails {
  li {
    //margin-bottom: $gutter;
  }
}

.ajaxified-cart-feedback {
  text-align: center;
  padding: 15px;
  margin-bottom: 25px;
  margin-top: 25px;
  background-color: $colorDenary;
  border: 1px solid $colorQuaternary;
  @include typestyle-heading4;
  color: $colorPrimary;

  &.error {
    padding: 15px 10px 10px 10px;
  }

  .fa {
    margin-right: 2px;
  }
}

.ajaxified-cart-feedback.success { 
  //color: $colorSenary;
}

.ajaxified-cart-feedback.error { 
  color: $errorRed; 
} 

.ajaxified-cart-feedback a {
  display: inline-block;
  color: $colorLinkTertiary;
  margin-top: 15px;
  @include typestyle-body3;
  letter-spacing: 0;
  background-image: url({{ 'shoppingbag.svg' | asset_url }});
  background-size: 20px 24.5px;
  background-position: top left;
  background-repeat: no-repeat;
  padding-left: 30px;
  padding-top: 3px;

  &:hover {
    color: $colorLinkTertiaryHover;
  }
}

.related-products {
  background-color: $colorDenary;
  padding-top: 65px;
  margin-top: 70px;

  h2 {
    color: $colorQuaternary;
    text-align: center;
    @include typestyle-heading2;
    margin-bottom: 18px;
  }

  .product-grid-item {
  }
}

@-moz-document url-prefix() { // Disable the select centering on Firefox due to bug
  select {
    text-indent: 0 !important;
  }
}

/*============================================================================
  #Info Pages
==============================================================================*/

.index-section {
    @include prefixer(animation-duration,2s,webkit ms o moz spec);
    @include prefixer(animation-delay,.2s,webkit ms o moz spec);
    @include prefixer(animation-interation-count,1,webkit ms o moz spec);

  &.hero {
    @include prefixer(animation-duration,.5s,webkit ms o moz spec);
    @include prefixer(animation-delay,0,webkit ms o moz spec);
    @include prefixer(animation-interation-count,1,webkit ms o moz spec);
  }
}
/*
.intro-wrapper {
    @include prefixer(animation-duration,2s,webkit ms o moz spec);
    @include prefixer(animation-delay,0,webkit ms o moz spec);
    @include prefixer(animation-interation-count,1,webkit ms o moz spec);
}
*/
/*.story-wrapper,
.mission-info-1,
.mission-info-2 {
    @include prefixer(animation-duration,2s,webkit ms o moz spec);
    @include prefixer(animation-delay,.5s,webkit ms o moz spec);
    @include prefixer(animation-interation-count,1,webkit ms o moz spec);
}*/

.wow {
  visibility: hidden;
}

.template-index {
  .shopify-section {
    padding-top: 40px;
    padding-bottom: 40px;
    text-align: center;

    .rte,
    p:last-child {
      margin-bottom: 0;
    }

    h2 {
      margin-top: -3px;
      margin-bottom: 30px;
      color: $colorSecondary;
    }

    &.hero {
      padding-top: 0;
      padding-bottom: 40px;

      img {
        margin-bottom: 38px;
      }

      h2 {
        color: $colorQuaternary;
        @include typestyle-heading2;
        margin-bottom: 14px;
      }

      .caption {
        background-image: url({{ 'confetti.svg' | asset_url }});
        background-size: 203px 48px;
        background-position: bottom center;
        background-repeat: no-repeat;
        padding-bottom: 80px;

        &.no-img {
          margin-top: 38px;
        }

        br {
          display: none;

          @include at-query ($min, $large) { // Show style at large breakpoint only
            display: block;
          }
        }
      }
    }

    &.gallery,
    &.featured-info,
    &.hero {
      p,
      .caption {
        @include typestyle-body3;
        max-width: 730px;
        margin: 0 auto;
      }
    }

    .featured-sections .grid__item {
      padding-bottom: $gutter;
    }

    &.featured-link-sections {
      background-color: $colorDenary;
      padding-bottom: 10px;

      h3,
      p {
        text-align: left;
      }

      img,
      h3,
      p.rte {
        margin-bottom: 15px;
      }

      h3 {
        @include typestyle-body6;
      }

      p {
        @include typestyle-heading7;

        &:last-child {
          @include typestyle-body9;
        }

        a {
          border-bottom: 1px solid $colorSeptary;
          padding-bottom: 1px;

          &:hover,
          &:focus {
            border-bottom: 1px solid $colorQuaternary;
          }
        }
      }
    }

    &.featured-collections {
      padding-bottom: 30px;

      .featured-sections {
        img {
          margin-bottom: 4px;
        }

        p {
          a {
            border: 1px solid lighten($colorTertiary, 10%);
            color: $colorSecondary;
            @include typestyle-body2;
            display: block;
            width: 100%;
            padding: 15px;
            
            &:hover,
            &:focus {
              background-color: $colorSecondary;
              color: $colorOctonary;
            }

          }
        }
      }
    }

     &.gallery {
      padding-top: 42px;
      
      {% if settings.hide_quotes_bg %}
        background-color: $colorDenary;
      {% else %}
        background-image: url({{ 'bg_dots.svg' | asset_url }});
        background-size: 1920px 315px;
        background-position: center center;
        background-repeat: repeat;
      {% endif %}

      h2 {
        color: $colorQuaternary;
        @include typestyle-heading2;
      }

      p {
        margin-bottom: 35px;

        &.author {
          @include typestyle-body2;
          margin-bottom: 55px;
        }
      }
    }

    &.featured-info {
      padding-top: 70px;
      padding-bottom: 70px;

      svg {
        max-width: 105px;
        margin-bottom: 35px;
        .st0 {fill: $colorQuaternary;}
      }

      p.caption {
        margin-bottom: 30px;
        @include typestyle-body3;
        line-height: 1.6;
      }

      p:last-child {
        @include typestyle-body9;
        line-height: 1.6;
      }

      a {
        border-bottom: 1px solid $colorSeptary;
        padding-bottom: 1px;

        &:hover,
        &:focus {
          border-bottom: 1px solid $colorQuaternary;
        }
      }
    }

    &.instagram-gallery {
      h2,
      h2 a {
        color: $colorSecondary;
      }

      #insta-feed {
        a:nth-child(7),
        a:nth-child(8),
        a:nth-child(9),
        a:nth-child(10)  {
          @media only screen and (min-width: 730px) and (max-width: 970px) {
            display: none;
          }
        }

        a:nth-child(9),
        a:nth-child(10)  {
          @media only screen and (min-width: 971px) and (max-width: 1200px) {
            display: none;
          }
        }

        img {
            margin: 0 2px -2px 2px !important;
            width: 140px !important;
            height: 140px !important;

          @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
            width: 224px !important;
            height: 224px !important;
          }
        }
      }
    }
  }
}

.template-page-about {
  .page-content {
    text-align: center;

    .section-header {
      margin-bottom: $gutter;
    }

    .rte {
      @include typestyle-body3;

      h2 {
        color: $colorQuaternary;
      }
    }

    .intro-wrapper {
      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        background-image: url({{ 'confetti_about.svg' | asset_url }});
        background-size: 1286px 385px;
        background-position: center center;
        background-repeat: no-repeat;
      }
    }

    .intro {
      padding-bottom: $gutter;

      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        padding-bottom: 80px;
      }

      .rte {
        max-width: 740px;
        margin: 0 auto;
      }
    }

    .story-wrapper {
      background-color: $colorDenary;
      padding-top: 60px;
      padding-bottom: 60px;
      
      .wrapper {
        max-width: 910px;
        margin: 0 auto;
      }

      &.rte p:last-child {
        @include at-query($max, $small) { // Show style at small breakpoint only
          margin-bottom: 25px;
        }
      }

      .custom-item {
        margin-bottom: $gutter;

        &:last-child {
          margin-bottom: 0;
        }

        .grid-uniform {
          .grid__item {
            @include at-query($max, $small) { // Show style at small breakpoint only
              margin-bottom: $gutter;
            }
          }
        }
      }

      .text-block-img {
        .grid__item {
          @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
            text-align: left;
          }
        }

        .text-block-img-text {
          @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
            padding-top: 20px;
          }
        }
      }

      .grid-img {
        line-height: 1;
      }

      p.caption {
        @include typestyle-body9;
        letter-spacing: 0;
        margin-top: 10px;
        color: $colorSeptary;
        line-height: 1.5;
      }

      .gallery-wrapper {
        max-width: 650px;
        margin: 0 auto;
      }
    }
  }
}

.template-page-mission {
  .page-content {
    .section-header {
      margin-bottom: 0;
    }

    .rte {
      h2 {
        color: $colorQuaternary;
        text-align: center;
      }
    }

    .intro-wrapper {
      padding-top: $gutter;
      padding-bottom: $gutter;

      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        padding-top: 70px;
        padding-bottom: 70px;
        background-image: url({{ 'confetti_mission.svg' | asset_url }});
        background-size: 1296px 282px;
        background-position: center center;
        background-repeat: no-repeat;
      }
    }

    .intro {
      text-align: center;
      
      .rte {
        max-width: 740px;
        margin: 0 auto;
        @include typestyle-body3;
      }
    }

    .mission1-wrapper,
    .mission2-wrapper {
      padding-top: 40px;
      padding-bottom: 40px;
      
      .wrapper {
        max-width: 910px;
        margin: 0 auto;
      }

      &.rte p:last-child {
        @include at-query($max, $small) { // Show style at small breakpoint only
          margin-bottom: 25px;
        }
      }

      .custom-item {
        margin-bottom: $gutter;

        &:last-child {
          margin-bottom: 0;
        }

        .grid-uniform {
          .grid__item {
            @include at-query($max, $small) { // Show style at small breakpoint only
              margin-bottom: $gutter;
            }
          }
        }
      }

      .grid-img {
        line-height: 1;
      }

      p.caption {
        @include typestyle-body9;
        letter-spacing: 0;
        margin-top: 10px;
        color: $colorSeptary;
        line-height: 1.5;
      }

      .quote {
        background-color: $colorOctonary;
        padding: 20px;
        text-align: center;

        p {
          text-align: left;
        }

        svg {
          max-width: 105px;
          margin-bottom: 20px;
          .st0 {fill: $colorQuaternary;}
        }

        @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
          padding: 40px 90px 40px 90px;
        }

        .read-more {
          text-align: center;
          @include typestyle-body6;
          color: $colorLinkSecondary;

          &:focus,
          &:hover {
            color: $colorLinkSecondary;
          }
        }
      }

      .grid-uniform {
        text-align: center;
        margin-left: -15px;

        .grid__item {
          padding-left: 15px;
        }
      }
    }

    .mission1-wrapper {
      background-color: $colorDenary;

      h2 {
        margin-top: 0;
      }

      .star {
        margin-top: -50px;
        margin-bottom: 30px;
        display: block;
        margin-left: 90%;
        width: 24px;
      }
    }
  }
}

.template-customers-account,
.template-customers-addresses  {
  .page-content-wrapper {
    h1.page-title,
    .account-btn {
      margin-left: $gutter;
    }
  }
}

/*============================================================================
  #Account Pages
==============================================================================*/

.template-customers-register,
.template-customers-login,
.template-customers-activate_account,
.template-customers-reset_password {
  .page-content {
    text-align: center;
  }
}

.template-customers-addresses,
.template-customers-account {
  h2,
  h2.h4 {
    margin-bottom: 1em;
    @include typestyle-heading6;
  }

  h3.h5,
  h3 {
    @include typestyle-body;
  }

  .account-btn {
    text-align: center;
    margin-bottom: $gutter;
  }

  .sidebar {
    @include at-query($max, $small) { // Show style at small breakpoint only
      text-align: center;
      margin-bottom: $gutter;
    }
  }
}

.template-customers-order,
.template-customers-account {
  .page-content {
    table {
      margin-bottom: $gutter;
    }
  }
}

.template-customers-order {
  .page-content {
    .btn--secondary,
    table {
      margin-bottom: $gutter;
    }
  }

  .page-content,
  .sidebar {
    h2.h4,
    h3.h5 {
      @include typestyle-heading6;
    }
  }
}

.template-404 {
  .page-content {
    padding-top: $gutter;
    text-align: center;

    img {
      margin: 0 auto;
      width: 200px;
    }
  }
}

/*============================================================================
  #Blog
==============================================================================*/

.template-article,
.template-blog {
  .page-content-wrapper {
    padding-top: 60px;
  }

  .page-content {
    h1 {
      text-align: center;
      margin-bottom: $gutter;
    }
  }

  .sidebar-section {
    background-color: $colorDenary;
    padding: $gutter 20px;
    text-align: center;

    @media only screen and (min-width: 1180px) {
      margin: 0 0 0 65px;
    }

    h3 {
      @include typestyle-body2;
      margin-bottom: 10px;
    }

    form,
    select {
      max-width: 300px;
    }
  }

  .blog-info {
    .sidebar-section {
      position: relative;
      background-image: url({{ 'confetti_blog2.svg' | asset_url }});
      background-size: 64px 32px;
      background-position: 0 10px;
      background-repeat: no-repeat;
    }

    h3,
    p.subtitle {
      color: $colorQuaternary;
    }

    h3 {
      @include typestyle-heading9;
      margin-bottom: 0;
      margin-top: 5px;
    }

    p.subtitle {
      @include typestyle-heading10;
      margin-bottom: 35px;

      a {
        text-decoration: none;
       }

      em {
        @include typestyle-heading5;
      }
    }

    img.confetti {
      position: absolute;
      right: 2px;
      top: 135px;
      margin-bottom: 0;
    }

    img {
      margin-bottom: 30px;
    }

    p {
      @include typestyle-body8;
      line-height: 1.5;
    }
  }

  .blog-search {
    .input-group .btn, .input-group .btn--secondary, .input-group .input-group-field {
      height: 33px;
    }

    .search-bar input {
      padding-left: 10px;
      padding-right: 10px;
      border-left: 1px solid $colorSenary;
      border-top: 1px solid $colorSenary;
      border-bottom: 1px solid $colorSenary;

      &:focus {
        background-color: $colorSenary;
      }
    }

    .search-bar button {
      color: $colorPrimary;
      padding-left: 10px;
      padding-right: 10px;

      border-right: 1px solid $colorSenary;
      border-top: 1px solid $colorSenary;
      border-bottom: 1px solid $colorSenary;

      &:hover,
      &:focus {
        color: $colorQuaternary;
      }
    }
  }

  .blog-tags {
    select {
      width: 100%;
      background-color: $colorOctonary;
      @include typestyle-body2;
      color: $colorSeptary;
      padding-top: 8px;
      padding-bottom: 6px;
    }
  }

  .blog-menu {
    padding-bottom: 30px;

    ul {
      @include ul-reset;
      @include typestyle-body5;

      li {
        margin-bottom: 3px;
      }
    }
  }

  .article-header {
    text-align: center;
    margin-bottom: $gutter;
    color: $colorSecondary;

    p,
    h2.h3 {
      margin-bottom: 0;
    }

    .date {
      @include typestyle-body10;
      margin-bottom: 10px;
    }

    h2.h3,
    h1 {
      @include typestyle-heading1; 
      margin-bottom: 5px;
    }

    .author {
      @include typestyle-body5;
    }

    a {
      color: $colorLinkQuaternary;

      &:hover,
      &:focus {
        color: $colorLinkQuaternaryHover;
      }
    }
  }

  ul.tags {
    text-align: center;
    @include typestyle-body6;
    margin-top: 5px;
  }

  .wp-image,
  .alignnone,
  .alignright,
  .alignleft {
    margin: 30px auto 30px auto;
    display: block;
    height: auto !important;
  }

  article,
  .article {
    .social-icons {
      text-align: center;
      color: $colorSecondary;
      margin-bottom: 0;

      li.label {
        @include typestyle-body9;
        text-transform: uppercase;
        margin-right: 10px;
      }

      li {
        margin-right: 4px;
        margin-bottom: 0;

        .icon {
          @include font-size(1.8);
        }
      }

      a {
        color: $colorLinkQuaternary;

        &:hover,
        &:focus {
          color: $colorLinkQuaternaryHover;
        }
      }
    }

    .article__featured-image img {
      margin-bottom: $gutter;
    }

    .read-more {
      text-align: center;
      margin-top: 30px;
      margin-bottom: 0;

      .btn {
        padding-left: 20px;
        padding-right: 18px;
      }
    }
  }

  .article {
    margin-bottom: 60px;
    padding-bottom: 60px;
    border-bottom: 1px solid $colorSenary;
  }

  .pagination {
    text-align: center;
    @include typestyle-body2;
  }
}

.template-blog {
  .article {
    .rte {
      margin-bottom: 0;
    }

    .social-icons {
      margin-top: $gutter;
    }
  }
}

.template-article {
  .article {
    .rte {
      margin-bottom: 50px;
    }
  }
}

.comments-heading {
  text-align: center;
  @include typestyle-heading8;
  border-top: 1px solid $colorSenary;
  margin-top: 30px;
  padding-top: 30px;
  margin-bottom: 28px;
}

#comments {
  .comment {
    background-color: $colorDenary;
    padding: 20px;

    span {
      @include typestyle-body5;
    }

    .rte {
      margin-bottom: 15px;
    }
  }

  h2.h3 {
    @include typestyle-heading8;
    margin-bottom: $gutter;
  }
}

/*============================================================================
  #Notes and Form Feedback
==============================================================================*/

.note,
.errors {
  //border-radius: $radius;
  //padding: 6px 12px;
  //margin-bottom: $gutter / 2;
  //font-size: 0.9em;

  ul {
    @include ul-reset;
  }
}

.note {
  border-color: $colorBorder;
}

.form-success {
  color: $successGreen;
  background-color: $successGreenBg;
  border-color: $successGreen;

  a {
    color: $successGreen;
    //text-decoration: underline;

    &:hover {
      //text-decoration: none;
    }
  }
}

.form-error,
.errors {
  color: $errorRed;
  background-color: $errorRedBg;
  border-color: $errorRed;

  a {
    color: $errorRed;
    //text-decoration: underline;

    &:hover {
      //text-decoration: none;
    }
  }
}

/*============================================================================
  #Cart Page
==============================================================================*/

.template-cart {
  form {
    label {
      color: $colorPrimary;
      margin-bottom: 1em;
      @include typestyle-body2;
      display: block;
    }

    .product-img {
      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        width: 250px;
        a {
          max-width: 200px;
        }
      }

      a {
        margin: 0 auto;
      }

      img {
        margin: 0 auto;
      }
    }

    td.product-price,
    td.product-quantity {
      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        text-align: center;
      }
    }   

    td.product-information {
      text-align: left;

      .h4 {
        @include typestyle-body3;
      }

      p {
        @include typestyle-heading6;
      }

      @include at-query ($min, $postSmall) { // Show style on larger than small breakpoints
        max-width: 200px;
      }

      p {
        margin-bottom: .5em;
      }
    }

    input[type="number"] {
      max-width: 100px;
    } 
  }

  label.gift-order-label {
    display: inline-block;
  }

  .update-cart {
    background-color: transparent;
    color: $colorLinkQuaternary;

    &:focus,
    &:hover {
      color: $colorLinkQuaternaryHover;
    }
  }
}

.cart__row.cart__header-labels {
  @include typestyle-body2;
  border-top: none;
}

.cart-empty {
  text-align: center;
}

.cart__row {
  @include typestyle-body8;
  border-top: 1px solid $colorBorder;

  &.top-checkout {
    border-top: none;

    .grid__item {
      @include at-query($max, $small) { // Show style at small breakpoint only
        text-align: center;
      }

      &.text-right {
        @include at-query($max, $small) { // Show style at small breakpoint only
          text-align: center !important;
          padding-bottom: 20px;
        }
      }
    }
  }

  &:first-child {
    @include at-query($max, $small) { // Show style at small breakpoint only
      border-top: none;
    }
  }

  .cart__remove {
    @include typestyle-body2;
    border: 1px solid $colorSecondary;
    padding: 6px 8px 6px 8px;
    display: inline-block;
    margin-top: 15px;

    &:before {
      font-family: $socialIconFontStack;
      @include font-size(1.0); 
      content: '\f00d';
      margin-right: 5px;
    }
  }

   .h4 {
    text-align: left;
   }

   .h3 {
    @include typestyle-heading8;
   } 

  .btn--secondary,
  .btn {
    margin-bottom: 1em;
  }
}

.cart__mini-labels {
  //font-size: em(12px);
}

.table--responsive td:before  {
  @include typestyle-body2;
  line-height: 1.2;
}

.table--responsive td.product-img:before  {
  display: none;
}

.cart-table td, .cart-table th {
  padding: 15px;
}

.template-cart {
  .table--responsive {
    @include at-query($max, $small) {

      &.full {
        margin-bottom: 20px;
      }

      td {
        clear: none;
      }

      td, th {
        padding: 5px;
      }

      td.product-img,
      td.product-information,
      td.product-quantity,
      td.total {
        &:before {
          padding-right: 0;
        }
      }

      td.product-quantity,
      td.total {
        &:before {
          //@include typestyle-heading3;
          margin-bottom: 3px;
          padding-right: 10px;
        }
      }

      td.product-img {
        width: 30%;
      }

      td.product-information{
        width: 70%;
      }

      td.product-quantity,
      td.total {
        padding-bottom: 0;

        &:before {
          float: none;
          padding-top: 10px;
          padding-right: 5px;
        }

        &:before,
        .h3 {
          display: inline-block;
        }
      }

      td.product-quantity {
        clear: left;
        text-align: left;
        width: 60%;

        input[type="number"] {
          max-width: 60px;
          @include font-size(1.3);
        } 
      }

      td.total {
        &.show-quantity {
          width: 40%;
        }
      
        .h3 {
          margin-bottom: 0;
          //@include typestyle-heading3;
        }
      }

      td.product-price {
        display: none;
      }

      td.total {
        .h3 {
          padding-top: 5px;
        }
      }

      .cart__remove {
        border: none;
        margin-top: 0;
        padding: 0;
      }
    }
  }
}

.mfp-content {
  div:first-child {
    padding-bottom: 30px !important;
    background-color: yellow;
  }

  .minmaxify-popup .minmaxify-btn, .minmaxify-btn {
    background-color: $colorQuaternary !important;
  }
}

// ==============================================================================
// #Vendor
// ==============================================================================

/*! fancyBox v2.1.5 fancyapps.com | fancyapps.com/fancybox/#license */
.fancybox-wrap,
.fancybox-skin,
.fancybox-outer,
.fancybox-inner,
.fancybox-image,
.fancybox-wrap iframe,
.fancybox-wrap object,
.fancybox-nav,
.fancybox-nav span,
.fancybox-tmp
{
  padding: 0;
  margin: 0;
  border: 0;
  outline: none;
  vertical-align: top;
}

.fancybox-wrap {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 8020;
}

.fancybox-skin {
  position: relative;
  background: #f9f9f9;
  color: #444;
  text-shadow: none;
  -webkit-border-radius: 4px;
     -moz-border-radius: 4px;
          border-radius: 4px;
}

.fancybox-opened {
  z-index: 8030;
}

.fancybox-opened .fancybox-skin {
  -webkit-box-shadow: none;
     -moz-box-shadow: none;
          box-shadow: none;
}

.fancybox-outer, .fancybox-inner {
  position: relative;
}

.fancybox-inner {
  overflow: hidden;
}

.fancybox-type-iframe .fancybox-inner {
  -webkit-overflow-scrolling: touch;
}

.fancybox-error {
  color: #444;
  font: 14px/20px "Helvetica Neue",Helvetica,Arial,sans-serif;
  margin: 0;
  padding: 15px;
  white-space: nowrap;
}

.fancybox-image, .fancybox-iframe {
  display: block;
  width: 100%;
  height: 100%;
}

.fancybox-image {
  max-width: 100%;
  max-height: 100%;
}

#fancybox-loading, .fancybox-close, .fancybox-prev span, .fancybox-next span {
}

#fancybox-loading {
  position: fixed;
  top: 50%;
  left: 50%;
  cursor: pointer;
  z-index: 8060;
}

#fancybox-loading div {
}

.fancybox-close {
  position: absolute;
  cursor: pointer;
  z-index: 8040;
}

.fancybox-nav {
  position: absolute;
  top: 0;
  width: 40%;
  height: 100%;
  cursor: pointer;
  text-decoration: none;
  background: transparent url({{ 'blank.gif' | asset_url }}); /* helps IE */
  -webkit-tap-highlight-color: rgba(0,0,0,0);
  z-index: 8040;
}

.fancybox-prev {
  left: 0;
}

.fancybox-next {
  right: 0;
}

.fancybox-nav span {
  position: absolute;
  top: 50%;
  cursor: pointer;
  z-index: 8040;
  //visibility: hidden;
}

.fancybox-prev span {
}

.fancybox-next span {
}

.fancybox-nav:hover span {
  visibility: visible;
}

.fancybox-tmp {
  position: absolute;
  top: -99999px;
  left: -99999px;
  max-width: 99999px;
  max-height: 99999px;
  overflow: visible !important;
}

/* Overlay helper */

.fancybox-lock {
  overflow: visible !important;
  width: auto;
}

.fancybox-lock body {
  overflow: hidden !important;
}

.fancybox-lock-test {
  overflow-y: hidden !important;
}

.fancybox-overlay {
  position: absolute;
  top: 0;
  left: 0;
  overflow: hidden;
  display: none;
  z-index: 8010;
}

.fancybox-overlay-fixed {
  position: fixed;
  bottom: 0;
  right: 0;
}

.fancybox-lock .fancybox-overlay {
  overflow: auto;
  overflow-y: scroll;
}

/* Title helper */

.fancybox-title {
  visibility: hidden;
  position: relative;
  text-shadow: none;
  z-index: 8050;
}

.fancybox-opened .fancybox-title {
  visibility: visible;
}

.fancybox-title-float-wrap {
  position: absolute;
  bottom: 0;
  right: 50%;
  margin-bottom: -35px;
  z-index: 8050;
  text-align: center;
}

.fancybox-title-float-wrap .child {
  display: inline-block;
  margin-right: -100%;
  padding: 2px 20px;
  background: transparent; /* Fallback for web browsers that doesn't support RGBa */
  background: rgba(0, 0, 0, 0.8);
  -webkit-border-radius: 15px;
     -moz-border-radius: 15px;
          border-radius: 15px;
  text-shadow: 0 1px 2px #222;
  color: #FFF;
  font-weight: bold;
  line-height: 24px;
  white-space: nowrap;
}

.fancybox-title-outside-wrap {
  position: relative;
  margin-top: 10px;
  color: #fff;
}

.fancybox-title-inside-wrap {
  padding-top: 10px;
}

.fancybox-title-over-wrap {
  position: absolute;
  bottom: 0;
  left: 0;
  color: #fff;
  padding: 10px;
  background: #000;
  background: rgba(0, 0, 0, .8);
}

/* Slick Slider */

.slick-slider {
    position: relative;
    display: block;
    box-sizing: border-box;
    -webkit-touch-callout: none;
    -webkit-user-select: none;
    -khtml-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
    -ms-touch-action: pan-y;
    touch-action: pan-y;
    -webkit-tap-highlight-color: transparent;
}

.slick-list {
    position: relative;
    overflow: hidden;
    display: block;
    margin: 0;
    padding: 0;

    &:focus {
        outline: none;
    }

    &.dragging {
        cursor: pointer;
        cursor: hand;
    }
}

.slick-slider .slick-track,
.slick-slider .slick-list {
    -webkit-transform: translate3d(0, 0, 0);
    -moz-transform: translate3d(0, 0, 0);
    -ms-transform: translate3d(0, 0, 0);
    -o-transform: translate3d(0, 0, 0);
    transform: translate3d(0, 0, 0);
}

.slick-track {
    position: relative;
    left: 0;
    top: 0;
    display: block;

    &:before,
    &:after {
        content: "";
        display: table;
    }

    &:after {
        clear: both;
    }

    .slick-loading & {
        visibility: hidden;
    }
}

.slick-slide {
    float: left;
    height: 100%;
    min-height: 1px;
    [dir="rtl"] & {
        float: right;
    }
    img {
        display: block;
    }
    &.slick-loading img {
        display: none;
    }

    display: none;

    &.dragging img {
        pointer-events: none;
    }

    .slick-initialized & {
        display: block;
    }

    .slick-loading & {
        visibility: hidden;
    }

    .slick-vertical & {
        display: block;
        height: auto;
        border: 1px solid transparent;
    }
}
.slick-arrow.slick-hidden {
    display: none;
}

.slick-list {
  .slick-loading & {
    background-image: url({{ 'loading.svg' | asset_url }});
    background-size: 70px 70px;
    background-position: center center;
    background-repeat: no-repeat;
  }
}

/* Dots */

.slick-dots {
  position: absolute;
  bottom: -20px;
  display: block;
  width: 100%;
  padding: 0;
  margin: 0;
  list-style: none;
  text-align: center;
}

.slick-dots li {
  position: relative;
  display: inline-block;
  width: 15px;
  height: 15px;
  margin: 0 4px;
  padding: 0;
  cursor: pointer;
}

.slick-dots li button {
  font-size: 0;
  line-height: 0;
  display: block;
  width: 20px;
  height: 20px;
  padding: 5px;
  cursor: pointer;
  color: transparent;
  border: 0;
  outline: none;
  background: transparent;
}

.slick-dots li button:hover,
.slick-dots li button:focus {
  outline: none;
}

.slick-dots li button:hover:before,
.slick-dots li button:focus:before {
  opacity: 1;
  color: $colorQuaternary;
}

.slick-dots li button:before {
  font-size: 6px;
  line-height: 20px;
  position: absolute;
  top: 0;
  left: 0;
  width: 20px;
  height: 20px;
  text-align: center;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-family: $socialIconFontStack;
  @include font-size(1.1); 
  content: '\f111';
  color: $colorQuinary;

  &:hover,
  &:focus {
    color: $colorQuaternary;
  }
}

.slick-dots li.slick-active button:before {
  content: '\f111';
  color: $colorQuaternary;
}

/* Arrows */

.slick-prev,
.slick-next {
  position: absolute;
  display: block;
  line-height: 0px;
  font-size: 0px;
  cursor: pointer;
  background: transparent;
  color: transparent;
  top: 50%;
  -webkit-transform: translate(0, -50%);
  -ms-transform: translate(0, -50%);
  transform: translate(0, -50%);
  padding: 0;
  border: none;
  outline: none;

  &:hover, &:focus {
    outline: none;
    background: transparent;
    color: transparent;
  }
}

// Bootstrap

/*!
 * Bootstrap v3.3.7 (http://getbootstrap.com)
 * Copyright 2011-2017 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 */

/*!
 * Generated using the Bootstrap Customizer (http://getbootstrap.com/customize/?id=9ba5a1b7c4d61b6e70c0b40e42b1f75e)
 * Config saved to config.json and https://gist.github.com/9ba5a1b7c4d61b6e70c0b40e42b1f75e
 */
/*!
 * Bootstrap v3.3.7 (http://getbootstrap.com)
 * Copyright 2011-2016 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 */

[role="button"] {
  cursor: pointer;
}

.dropup,
.dropdown {
  position: relative;
}

.dropdown-toggle:focus {
  //outline: 0;
}

.dropdown-menu {
  position: absolute;
  top: 100%;
  left: 0;
  z-index: 1000;
  display: none;
  float: left;
  min-width: 160px;
}

.dropdown-menu.pull-right {
  right: 0;
  left: auto;
}

.dropdown-menu > li > a {
  display: block;
  clear: both;
  white-space: nowrap;
}

.dropdown-menu > li > a:hover,
.dropdown-menu > li > a:focus {
  text-decoration: none;
}

.dropdown-menu > .active > a,
.dropdown-menu > .active > a:hover,
.dropdown-menu > .active > a:focus {
  text-decoration: none;
  outline: 0;
}

.dropdown-menu > .disabled > a,
.dropdown-menu > .disabled > a:hover,
.dropdown-menu > .disabled > a:focus {
}

.dropdown-menu > .disabled > a:hover,
.dropdown-menu > .disabled > a:focus {
  text-decoration: none;
  cursor: not-allowed;
}

.open > .dropdown-menu {
  display: block;
}

.open > a {
  outline: 0;
}

.dropdown-menu-right {
  left: auto;
  right: 0;
}

.dropdown-menu-left {
  left: 0;
  right: auto;
}

.dropdown-header {
  display: block;
  white-space: nowrap;
}

.dropdown-backdrop {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  top: 0;
  z-index: 990;
}

.pull-right > .dropdown-menu {
  right: 0;
  left: auto;
}

.dropup .dropdown-menu,
.navbar-fixed-bottom .dropdown .dropdown-menu {
  top: auto;
  bottom: 100%;
  margin-bottom: 2px;
}

@media (min-width: 768px) {
  .navbar-right .dropdown-menu {
    left: auto;
    right: 0;
  }
  .navbar-right .dropdown-menu-left {
    left: 0;
    right: auto;
  }
}

.clearfix:before {
  content: " ";
  display: table;
}

.center-block {
  display: block;
  margin-left: auto;
  margin-right: auto;
}

.pull-right {
  float: right !important;
}

.pull-left {
  float: left !important;
}

.hide {
  display: none !important;
}

.show {
  display: block !important;
}

.invisible {
  visibility: hidden;
}

.text-hide {
  font: 0/0 a;
  color: transparent;
  text-shadow: none;
  background-color: transparent;
  border: 0;
}

.hidden {
  display: none !important;
}

.affix {
  position: fixed;
}

// ==============================================================================
// #FancyBox Customizations
// ==============================================================================

fancybox-error,
.fancybox-title {
  @include typestyle-body;
}

.fancybox-close,
.fancybox-prev span, 
.fancybox-next span {
  opacity: 0.8;
}

.fancybox-nav span {
  width: 50px;
  height: 50px;
  margin-top: -18px;
}

.fancybox-prev span {
  left: 10px;

  &:before {
    font-family: $socialIconFontStack;
    @include font-size(2.5); 
    content: '\f104';
    text-align: center;
    display: block;
    color: $colorQuaternary;
    background-color: $colorOctonary;
    width: 50px;
    height: 50px;
    vertical-align: middle;
    line-height: 50px;
    border-radius: 50px;
    padding-right: 4px;
  }
}

.fancybox-next span {
  right: 10px;
  /*
  background-image: url({{ 'next.svg' | asset_url }});
  background-size: 50px 50px;
  background-position: center center;
  background-repeat: no-repeat;
  */

  &:before {
    font-family: $socialIconFontStack;
    @include font-size(2.5); 
    content: '\f105';
    text-align: center;
    display: block;
    color: $colorQuaternary;
    background-color: $colorOctonary;
    width: 50px;
    height: 50px;
    vertical-align: middle;
    line-height: 50px;
    border-radius: 50px;
    padding-left: 6px;
  }
}

.fancybox-close {
  top: 10px;
  right: 10px;
  width: 50px;
  height: 50px;

  &:before {
    font-family: $socialIconFontStack;
    @include font-size(1.5); 
    content: '\f00d';
    text-align: center;
    display: block;
    color: $colorQuaternary;
    background-color: $colorOctonary;
    width: 50px;
    height: 50px;
    vertical-align: middle;
    line-height: 50px;
    border-radius: 50px;
    padding-left: 2px;
  }
}

#fancybox-loading {
  margin-top: -35px;
  margin-left: -35px;

  div {
    width: 70px;
    height: 70px;
    background-image: url({{ 'loading.svg' | asset_url }});
    background-size: 70px 70px;
    background-position: center center;
    background-repeat: no-repeat;
  }
}


// ==============================================================================
// #Slick Slider customizations
// ==============================================================================

/* prevent flash */
.slider-gallery {
  display: none;
}
.slider-gallery.slick-initialized{
  display: block;
}

.slick-slider {
  margin: 0;
}

.slider-gallery li {
  position: relative;
}

.slick-prev, .slick-next, .slick-prev:hover, .slick-next:hover {
  width: 60px;
  height: 60px;
  z-index: 2;

  .sidebar-section & {
    width: 40px;
    height: 40px;
  }
}

.slick-prev,
.slick-prev:hover {
  left: -30px;

  @include at-query($max, $small) { // Show style at small breakpoint only
    left: -20px;
  }
  
  &:before {
    opacity: 1;
    background-image: url({{ 'prev.svg' | asset_url }});
    background-size: 40px 40px;
    display: inline-block;
    width: 40px; 
    height: 40px;
    content:"";
  }
}

.slick-next,
.slick-next:hover {
  right: -25px;

  @include at-query($max, $small) { // Show style at small breakpoint only
    right: -20px;
  }

  &:before {
    opacity: 1;
    background-image: url({{ 'next.svg' | asset_url }});
    background-size: 40px 40px;
    display: inline-block;
    width: 40px; 
    height: 40px;
    content:"";
  }
}

/*============================================================================
  #Bootstrap customizations
==============================================================================*/

.dropdown-menu {
  background-color: $colorOctonary;
  padding: 20px 25px;
  border: 1px solid $colorSenary;
  min-width: 135px;
  left: 50%;
  right: auto;
  text-align: left;
  transform: translate(-50%, 0);
  -webkit-transform: translate(-50%, 0);
  -ms-transform: translate(-50%, 0);

  li {
    margin-bottom: 5px;

    &:last-of-type {
      margin-bottom: 0;
    }
  }
}

/*============================================================================
  #Ajax Cart Styles (conditionally loaded)
==============================================================================*/

.ajaxcart__row {
  border-bottom: 1px solid $colorDrawerBorder;
}


/*================ Quantity Selectors ================*/

.js-qty__adjust {
  &:hover,
  &:focus {
    color: $colorPrimary;
  }
}

.js-qty__adjust--plus {
  right: 0;
  border-left: 1px solid $colorBorder;
}

.js-qty__adjust--minus {
  left: 0;
  border-right: 1px solid $colorBorder;
}

/*================ Quantity Selectors in Ajax Cart ================*/

.ajaxcart__qty-num {
  border-color: $colorDrawerBorder;
  color: $colorDrawerText;
}

.ajaxcart__qty-adjust {
  color: $colorDrawerText;
}

.ajaxcart__qty--plus {
  border-color: $colorDrawerBorder;
}

.ajaxcart__qty--minus {
  border-color: $colorDrawerBorder;
}